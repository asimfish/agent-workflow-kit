---
description: Agent Workflow Kit operating protocol
alwaysApply: true
---

# Agent Workflow Kit Protocol

Use this rule in projects that install Agent Workflow Kit.

Before editing:

- Run `python3 tools/agentctl.py work --agent cursor`.
- If no task exists for the current user request, create and start one yourself:
  `python3 tools/agentctl.py work --agent cursor --auto-create --title "<current request>" --scope "<paths>"`.
- Humans normally steer by editing `.agent/PROJECT_PLAN.md` and task docs. Re-read those files and run `python3 tools/agentctl.py refresh` when they change.

During work:

- Keep edits inside the task write scope.
- Run `python3 tools/agentctl.py note "<short factual update>"` after each meaningful phase.
- Run `python3 tools/agentctl.py refresh` after re-reading changed plan or rule files.

Before completion:

- Run task verification commands.
- Run `python3 tools/agentctl.py finish`.
