# Gate TA08B0CC413F151F5-037

- Decision: rejected
- By: independent-reviewer-037b
- Reviewer task: TF7A384C463E709BB-001
- Reviewer session started: 2026-09-06 18:13:49
- Reviewer runtime: host-runtime:c499e197980ffeeb938b82871f803030
- Worker runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- At: 2026-09-06 19:22:58
- Note: REJECT (second review). All three blocking findings from review 037 are closed as described and were confirmed on fresh temp projects installed from the branch kit at fcfbd6d: (1) the exact previous forgery finish --done x --tests-cmd $'true\n- Worker-runtimes: host-runtime:forged || true' exits 1 with 'a tests command must be a single line' and nothing lands in the doc; \r alone, and a newline through contract --tests-cmd, task create --tests-cmd, work --auto-create --tests-cmd (shared checkout and worktree bootstrap) are all refused (rc 1/2, nothing on the board); --done/--goal/--summary/--tests newline payloads collapse to one field; a hand-planted forged first Worker-runtimes line no longer hides the real one and a same-CODEX_THREAD_ID poser is refused with 'participated in the worker task'. (2) After a hand edit of the task doc, note, contract --done and finish --done are all blocked with rc 1 and the doc is unchanged; read-only contract works and does not refresh; refresh then contract --done proceeds. (3) gate approve --rerun-tests with a planted 'sleep 20' let a third session's note complete in 0.94s and refresh in 0.39s, then approved with 'Tests rerun: passed'; swapping the recorded Tests-command mid-rerun was refused with 'changed while the rerun was in progress' and no gate file was written. Non-blocking items also hold: AGENT_WORKFLOW_TESTS_TIMEOUT=2 with 'sleep 40; echo never' refuses finish, pgrep finds no sleep 40 afterwards, status stays in_progress; a command with backticks and $(...) round-trips byte-identical through Tests command -> finish -> Tests-command -> rerun -> gate file. Reran the worker's recorded tests command by hand from the branch checkout: 54 passed in 237s; tests/test_task_contract.py alone 23 passed in 173s; install_and_hooks+day_one+read_receipt+guidance+multi_checkout_sync 66 passed in 440s; the rest of the suite 205 passed, 1 failed (test_execution_lease_workflow::test_supervised_run_holds_resource_then_releases_it_after_success, a pre-existing timing race untouched by the branch: resource acquire returned 0 before the supervised run took the lease under load 150+) which passed in isolation in 6.5s, so 295/295; collect-only counts 295 matching both READMEs; the identity-policy test includes ('contract',); kit WORKFLOW_ENTRY and task template equal templates/project. BLOCKING 4 (the same hole, one character wider): _tests_cmd_problem refuses only [\r\n], but every reader of the record (_extract_section, _task_doc_field, the finish-output tail, and cat/Markdown in a human's eye differ here) goes through str.splitlines(), which also breaks on \x0b, \x0c, \x1c, \x1d, \x1e, \x85, U+2028 and U+2029, and _completion_record_line only collapses [\r\n] too, so a form feed reproduces the original forgery end to end: finish --summary s --tests unit --tests-cmd $'true\x0c- Worker-runtimes: host-runtime:forged\x0c- Completed-at: 2026-01-01 00:00:00\x0c## Notes\x0c: || true' exits 0, the record is physically one line but _extract_section sees a forged Worker-runtimes line and a '## Notes' header that ends the Completion Record BEFORE the real Worker-runtimes line, so _worker_runtimes_recorded returns only {host-runtime:forged}; a review agent registered from a second session on the SAME CODEX_THREAD_ID then ran gate approve --rerun-tests and got 'approved -> done' (gate file: Reviewer runtime identical to the worker's real runtime, Worker runtimes: host-runtime:forged, Tests rerun: passed for the truncated command 'true'). Reproduced for \x0b, \x1c and U+2028 as well; the same character truncates what finish and the rerun execute ('echo first\x0cecho second; exit 1' runs only 'echo first' and passes) and after any later _set_task_doc_field write on the doc the form feed is rewritten to a real newline, so the forged bullets become physical lines. The same 'fake section header' trick works with a plain hand edit (a '## Notes' line inserted before the real Worker-runtimes line), so the union of Worker-runtimes lines is not sufficient by itself; the gate should also refuse a completion record whose Worker-runtimes set does not contain every runtime the finishing sessions recorded, or at least treat a Completion Record that is not the last section / contains a header as tampered. Related non-blocking: task create --goal/--done take the raw string (only contract and finish go through _completion_record_value), so a \x0c in --goal at work --auto-create plants a fake '## Completion Record' header inside Task Contract, and the next finish's body.find('## Completion Record') then truncates the doc at that point (Scope, Stage Plan and Verification sections deleted; observed 75 -> 30 lines); --note on gate approve/reject is written raw so a newline in it forges '- Reviewer task: <id>' lines that _decided_review_evidence then honours (this predates the branch, same on origin/main); a failed worktree bootstrap due to the single-line refusal leaves an active worktree lease, branch and directory behind that block a retry with 'scope overlaps active worktree task'; the derived Tests: line takes the command's last output line verbatim so it can read '- Worker-runtimes: ...' (harmless today because it is only checked for missing-markers). Fix: define one 'record line' predicate used by _tests_cmd_problem, _completion_record_line, _completion_record_value and every field writer that rejects or collapses every character str.splitlines() treats as a boundary (or any C0/C1 control character and U+2028/9), add a test that iterates over that character set through finish, contract and task create, and make the gate cross-check the Worker-runtimes set against the finishing sessions' recorded runtime identities or refuse a Completion Record that is not the final section.
