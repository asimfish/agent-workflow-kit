# Gate TA08B0CC413F151F5-037

- Decision: rejected
- By: independent-reviewer-037e
- Reviewer task: TBEF663A7F4F4F28D-001
- Reviewer session started: 2026-09-07 05:22:13
- Reviewer runtime: host-runtime:e360bb0c3f7c08ad5323347aa971f347
- Worker runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- At: 2026-09-07 05:47:11
- Note: REJECT (fourth review). Confirmed closed on fresh temp projects installed from the branch kit at fdc95ff: reviewer 037c's exact note payload (real newlines, a '## completion record' header and forged record lines) lands as one Stage Log line and one progress.md line, the doc keeps exactly one record header, finish --tests-cmd 'echo REAL-COMMAND-RAN' then records the real command and runtime and _completion_record_problem is ''; the same payload through --title at work --auto-create and task create is one line in the heading, the TASKS.md row and the PROJECT_PLAN bullet (no new '## ' line anywhere); a --takeover --reason with line breaks from a second session on a git clone is one line in Stage Log and progress.md; agents add --id and task create --owner with a newline exit 2; a hand-planted exact-case '## Completion Record' under Stage Log makes finish exit 1 ('2 Completion Record sections', and 'not the last section' when it is the only one, lower-case too) with the document byte-identical afterwards and '## Verification' intact, while a mixed-case header in the legitimate last position is rewritten canonically; cross-clone on a clone with no session records, a different-conversation reviewer's gate approve --rerun-tests is refused by structure for both a '## Notes' line inside the record and a planted lower-case record, and the documented limit holds: replacing only the Worker-runtimes VALUE in a well-formed last-and-only record is approved on the clone by a different conversation and by the worker's own conversation alike. Reran the worker's recorded tests command by hand from the branch checkout: 64 passed in 191.74s, EXIT 0; read_receipt+guidance+multi_checkout_sync 35 passed in 133.75s; collect-only 305 matches both READMEs; kit WORKFLOW_ENTRY.md and _template.md equal the template copies. BLOCKING, same class (a forged value in the task document that the gate trusts), and it contradicts the sentence this version adds to README, README_CN and the CHANGELOG ('inside one repository the gate reads the worker's own session records and a same-conversation self-review is refused whatever the committed text says'): on the SAME checkout, the worker's own conversation approves its own task after a value-only edit. Steps, every command with the same AGENT_WORKFLOW_SESSION_ID=w CODEX_THREAD_ID=thread-w: work --agent codex --auto-create --title 'real work' --scope src/r/ --done 'it works'; finish --summary 'did it' --tests-cmd 'echo REAL' (record: Worker-runtimes host-runtime:17fc5b9b...); replace that one value with host-runtime:forged (structure untouched); agents add --id poser --role review; work --agent poser --auto-create --type review --title 'review <task>' --scope .agent/; refresh; gate approve --task <task> --by poser --rerun-tests --note self -> 'approved -> done', gate file 'Reviewer runtime: host-runtime:17fc5b9b...' (the real worker runtime) and 'Worker runtimes: host-runtime:forged'. Root cause: a session record is one file per (session key, checkout) in .git/agent-workflow/sessions and _save_session overwrites it when that key starts the review task, so after step 5 no session row names the worker task, the gate's union adds nothing and only the committed value remains; the runtime is not persisted anywhere else (board.json keeps no runtime, the task lock is unlinked at finish). Controls: with the record untouched the same conversation is refused ('participated in the worker task'); with the edit but a DIFFERENT session id and the same thread id (AGENT_WORKFLOW_SESSION_ID=w2 CODEX_THREAD_ID=thread-w, exactly the form every poser test in test_task_contract.py uses, whose comment says 'The worker session on this checkout still names the runtime') it is refused by the union, which is why the suite does not see this. Fix: at finish persist the task's worker runtimes in a record the next 'work' cannot overwrite (a task-keyed file under the git common dir, or archive the session record under a task-suffixed name when its key moves on) and union it at the gate; add a test in which the same session id finishes and then reviews; and correct the README/README_CN/CHANGELOG sentence to what actually holds (the same-repository check is only as strong as the session records that still name the task). enforcement.md: deferring its wording to a follow-up docs task is acceptable for one more PR, but the sentence the READMEs and CHANGELOG replaced it with is itself inaccurate and must be fixed in this task. NON-BLOCKING: the plain claim 'work --agent <id> --task <id>' (cmd_start) skips _agent_id_problem, so an unregistered id with a newline becomes the board owner and splits the TASKS.md row over two physical lines (the task doc Owner line is unaffected; TASKS.md is not gate-read), and _render_task_views writes the plan bullet title/owner raw from board.json; handoff and guidance docs write '- Summary:' and the '### Plan' block raw under .agent/handoffs/ (not gate-read); a tests command that edits the task document and runs 'agentctl refresh' between finish's structure check and its write makes finish rewrite from the planted first header and drop '## Verification' (72 -> 62 lines) with the truncated doc passing the tamper check, a robustness gap rather than a forgery since the record it writes is genuine and the command is visible in Tests-command (re-check the structure under the lock before writing); _extract_section and _set_task_doc_field inline strip().lower() == header.lower() instead of calling _is_section_header (equivalent behavior). Skipped: the full 305-test suite (six of the nine suites run here, the previous reviewer's logs show the nine passing) and a Unicode case-fold sweep of header variants (reader and checks now share one predicate, so they agree by construction).
