# Gate TA08B0CC413F151F5-037

- Decision: rejected
- By: independent-reviewer-037
- Reviewer task: T0211939C246A038C-001
- Reviewer session started: 2026-09-06 07:00:14
- Reviewer runtime: host-runtime:716cc15ac179ddfc2f9bc432fc45de0e
- Worker runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- At: 2026-09-06 08:18:04
- Note: REJECT. Reproduced on fresh temp projects installed from the branch kit: the headline rules hold (finish refused without a Definition of Done with rc 1 and the contract hint; finish --done lands in the doc; a failing or timed-out tests command refuses finish and leaves in_progress with no Completed-at; a passing one records Tests-command/Tests-exit/Tests-duration; gate approve --rerun-tests passes and writes the DoD/command/rerun lines, refuses with no gate file and status review on failure, rc 1 with nothing recorded, and says 'not rerun' without the flag; a review task closes on its recorded decision and is refused before one; leading-dash values survive the worktree bootstrap as --flag=value; field edits stay inside their section and match labels case-insensitively; the template's empty Tests command bullet reads as none; README count 288 matches collect-only; kit WORKFLOW_ENTRY and task template equal their template copies; README_CN is a faithful UTF-8 translation; test_task_contract.py is not in the Windows CI list). Reran the worker's tests command by hand from the branch checkout plus test_read_receipt and test_guidance_workflow: 68 passed in 666s; the rest of the suite: 220 passed in 2367s; 288/288, no flaky reruns needed. BLOCKING 1: Tests-command is written into the completion record unsanitized (unlike --summary/--tests, which go through _completion_record_value), so finish --done x --tests-cmd $'true\n- Worker-runtimes: host-runtime:forged || true' exits 0 and injects a forged Worker-runtimes line ahead of the real one; the gate's re.search takes the first match, and a second workflow session on the SAME CODEX_THREAD_ID registered a review agent, opened a review task and gate-approved its own task (gate file shows Reviewer runtime identical to the real worker runtime in the completion record). The identical payload through --summary is still caught ('reviewer host runtime participated in the worker task'). This defeats the independence guarantee the kit advertises; the same truncation means --rerun-tests reruns only the first line of a multi-line command. Fix: collapse the command with _completion_record_value or refuse newlines in --tests-cmd, read every Worker-runtimes line at the gate, and add a --tests-cmd variant of test_gate_requires_active_independent_reviewer_session. BLOCKING 2: agentctl contract --done/--goal/--tests-cmd refreshes doc_hashes for every required document without calling _check_receipt first; after a human hand-edited the task doc (and separately AGENTS.md), note was blocked as expected, then one contract --done x returned 0 and the next note proceeded with no 'documents changed' warning. finish --done is safe because cmd_complete checks the receipt before the write; a read-only contract does not refresh. Check the receipt before writing, as note does. BLOCKING 3: gate approve --rerun-tests executes the command while holding .git/agent-workflow/sessions.lock (10s acquire timeout); with a 25s command a concurrent note in the same repo died after 10s with an uncaught TimeoutError traceback, so with the kit's own 15-minute command every other agent's ledger command in every worktree fails for the duration. Run the command before taking the lock, as finish does, then re-validate under it. NON-BLOCKING: backticks in a tests command are rewritten to single quotes in the Verification section, so test "`echo 3`" = "3" passed at finish but the re-finish after a reject ran the stored test "'echo 3'" = "3" and was refused; compound commands leave grandchildren alive on timeout (sleep 37 from 'sleep 37; echo never' survived, a single sleep 41 was killed) - use start_new_session and killpg; a nested-list or indented Definition of Done reads as empty so finish refuses although the field is filled; Tests: derives from the last output line even when it is stderr noise; a hand-forged Tests-exit: 0 satisfies _completion_evidence_problems but that is no weaker than the prose line and the rerun is the defense. The Prove2Me framing, the docs, and 16 new tests are good; the record-integrity hole must close before the contract can be the audit surface.
