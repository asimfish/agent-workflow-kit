# Gate TA08B0CC413F151F5-037

- Decision: rejected
- By: independent-reviewer-037h
- Reviewer task: T3304B5F437A610F6-001
- Reviewer session started: 2026-09-08 19:15:49
- Reviewer runtime: host-runtime:15276ea9ac87a19fff6ccf51aa94a947
- Worker runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- At: 2026-09-08 19:50:45
- Note: REJECT (sixth review). Confirmed closed on fresh temp projects installed from the branch kit at 171eaef: reviewer 037f's exact no-finish scenario (AGENT_WORKFLOW_SESSION_ID=w CODEX_THREAD_ID=thread-w throughout: work --auto-create --done; hand-written well-formed record with Worker-runtimes host-runtime:forged and status review; sessions release; agents add poser; work poser --type review; refresh; gate approve --rerun-tests) ends in 'reviewer host runtime participated in the worker task and is not independent', status review, no gate file, and .git/agent-workflow/task-runtimes/<task>.json exists right after work with the real runtime; 037e's same-session finish plus value-only edit is refused the same way; alice starts and releases, bob finishes, alice is refused and carol approves; no over-refusal: one reviewer approves T1 then T2 and rejects then approves T3, and focus --task X, capsule --task X, task show X and worktree create --task X by a supervisor write nothing under task-runtimes (the file appears only when the worker claims X in the worktree, shared common dir); reconcile archive --days 0 leaves every task-runtimes file in place (never deleted by any command; non-blocking, 151 bytes per task); cross-clone exactly as documented: the value-only edit committed and git-cloned is approved on the clone by the worker's own conversation (Reviewer runtime = the real worker runtime, Worker runtimes: host-runtime:forged) while a '## Notes' line inside the record and a doubled lower-case header are refused there by structure. Reran the worker's recorded tests command by hand from the branch checkout together with read_receipt, guidance and multi_checkout_sync in one run: 103 passed in 1782.63s, EXIT 0 (the worker's three suites collect 68, test_task_contract.py 37, under load 30-300, no flaky reruns needed); collect-only 309 matches both READMEs; kit WORKFLOW_ENTRY.md and all four _template.md equal the template copies. BLOCKING, same class (a hand-written record the gate trusts on the same repository because the task-keyed record forgot a runtime the live session union knew) and the same root cause shape as 037e/037f: 'every save of a task-bound session' is not true in code, two writers bypass _save_session. _sessions_heartbeat (the command the read-only PreToolUse hook runs on every read tool call) calls _record_runtime_identity and then _save_json(_session_path(...)) directly, so a runtime that joins a session through heartbeats only lands in the row's runtime_identities and never in task-runtimes/<task>.json; _sessions_release also writes the row with _save_json directly, so the release that README, README_CN, the CHANGELOG, the _task_runtimes_path docstring and this completion record all list as recording the session's runtimes records nothing. Steps on one fresh checkout: AGENT_WORKFLOW_SESSION_ID=w CODEX_THREAD_ID=thread-a work --agent codex --auto-create --title 'real work' --scope src/r/ --done 'it works' (task-runtimes = [A]); AGENT_WORKFLOW_SESSION_ID=w CODEX_THREAD_ID=thread-b sessions heartbeat -> the w row lists [A, B], task-runtimes still [A]; control: thread-b under AGENT_WORKFLOW_SESSION_ID=w2 opens a review task and gate approve is refused with 'participated in the worker task' (the live union counts B); hand-write the well-formed record (Worker-runtimes host-runtime:forged, status review, _completion_record_problem ''); thread-b under w: sessions release <w key> --reason handoff (task-runtimes byte-identical, recorded_at unchanged); agents add --id poser --role review; work --agent poser --auto-create --type review --scope .agent/gates/ (the w row is overwritten, no row names the task); refresh; gate approve --task <task> --by poser --rerun-tests --note self -> 'approved -> done', gate file Reviewer runtime = B, 'Worker runtimes: A, host-runtime:forged', 'Tests rerun: passed'. The same with no forgery at all in the handoff shape: alice claims, thread-b heartbeats under alice's session id, alice releases, bob claims and finishes (record and task-runtimes = alice, bob), thread-b under alice's session id opens a review task and approves ('approved -> done', Reviewer runtime = B). Any _save_session on the row after the heartbeat (a note, refresh or finish by either runtime) does record B, so the gap needs the no-finish or handoff shape, and B is a distinct conversation that only read under the worker's session, which makes this narrower than 037f; it is still the live union and the persistent record disagreeing about who held the session (the very inconsistency 037e and 037f were rejected on), reachable through the kit's own CLI with the same env shape every test in test_task_contract.py uses, no control character and no access to .git/ needed, and the four documents state a recording that does not happen. Fix: make _sessions_heartbeat and _sessions_release append through _record_task_runtimes (or go through _save_session) so every writer of a task-bound row records, add a test in which a second runtime joins by heartbeat, the session is released and the joiner reviews (both shapes), and make the README/README_CN/CHANGELOG list match the code. enforcement.md: deferring 'differ from every runtime that participated in the worker task' to the docs task is acceptable for one more PR; with the fix above it is true inside one repository. NON-BLOCKING: task-runtimes files are never deleted (archive, reconcile, worktree release), _load_json returns {} for a corrupt file (by-hand boundary), and the E5 supervisor-approves-X step was not completed (my planning claim needed an initial commit); the binding question is answered by the absent file. Skipped: the remaining 206 of 309 tests (six suites run here), the Windows job, gate reconcile-github.
