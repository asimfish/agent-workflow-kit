# Gate TA08B0CC413F151F5-037

- Decision: rejected
- By: independent-reviewer-037f
- Reviewer task: T07C23F24446DE56B-001
- Reviewer session started: 2026-09-07 06:08:22
- Reviewer runtime: host-runtime:5a07f78991d21b0fca4cc289a5e4fc01
- Worker runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- At: 2026-09-07 06:31:42
- Note: REJECT (fifth review). Confirmed closed on fresh temp projects installed from the branch kit at f424869: reviewer 037e's exact seven steps (AGENT_WORKFLOW_SESSION_ID=w CODEX_THREAD_ID=thread-w throughout: work --auto-create --done; finish --tests-cmd 'echo REAL'; replace only the Worker-runtimes value with host-runtime:forged; agents add poser; work poser --type review; refresh; gate approve --rerun-tests) end in 'reviewer host runtime participated in the worker task and is not independent', status review, no gate file; .git/agent-workflow/task-runtimes/<task>.json exists after step 2 with the real runtime and is byte-identical after step 5 although no session row names the worker task any more; a different conversation then approves the same edited record and its gate file lists both the real runtime (from the local record) and host-runtime:forged, so the edit even leaves a trace. sessions release, reconcile migrate, reconcile archive --days 0 and worktree release leave the file byte-identical; no CLI path deletes or rewrites it, only a hand rm under .git/ does (done: the same conversation is then approved with Reviewer runtime equal to the real worker runtime, the acknowledged by-hand boundary). The record follows the task through merge-back: a code task finished inside its task worktree wrote the record into the shared common dir, and from the planning checkout after reconcile merge-back, with the worktree's session row removed by hand so only the task-keyed record could speak, the same conversation was refused and a different one approved with --rerun-tests. Task ids accepted by task create ([A-Z][A-Z0-9]*-[A-Z0-9][A-Z0-9._-]*) pass through _safe_segment unchanged except for trailing dashes: T-1- and T-1 share T-1.json, the union then over-refuses (alice, who finished only T-1-, cannot review T-1) and never under-refuses, so the collision is fail-closed and cosmetic (the file's 'task' field is the last writer's). Also confirmed: a tests command that plants an exact-case '## Completion Record' under Stage Log and runs agentctl refresh makes finish exit 1 with '2 Completion Record sections (the document changed while the tests command ran)', status in_progress and '## Verification' intact (037e's truncation gap closed); plain work --agent 'codex<newline>- Reviewer task: T-X' --task T-000 exits 2 and changes nothing; a hand-planted multi-line title and owner in board.json render as one plan bullet and one TASKS.md row after reconcile render. Cross-clone boundary holds exactly as the README says: the value-only edit committed and git-cloned is approved on the clone by the worker's own conversation (Reviewer runtime = real worker runtime, Worker runtimes: host-runtime:forged) while the unedited record refuses it there. Reran the worker's recorded tests command by hand from the branch checkout: 66 passed in 177.51s, EXIT 0; the eight suites install_and_hooks, day_one, read_receipt, guidance, multi_checkout_sync, merge_back, worktree, multi_session: 146 passed in 653.09s, EXIT 0, load 7-13, no flaky reruns needed; test_task_contract.py collects 35 with both new tests; collect-only 307 matches both READMEs; kit WORKFLOW_ENTRY.md and all four _template.md equal the template copies. BLOCKING, the same class (a forged record in the committed task document that the gate trusts on the same checkout) and the same root cause 037e named (the session file is the only thing tying a runtime to a task until finish, and the next work of the same key overwrites it): the task-keyed record is written ONLY by finish, so a conversation that never runs finish leaves no local record at all. Steps, every command with AGENT_WORKFLOW_SESSION_ID=w CODEX_THREAD_ID=thread-w on one fresh checkout: work --agent codex --auto-create --title 'real work' --scope src/r/ --done 'it works'; no finish: rewrite the template's empty '## Completion Record' section from its header by hand with a well-formed record (Summary, Tests, Tests-command: echo REAL, Tests-exit: 0, Tests-duration, Worker-runtimes: host-runtime:forged, Completed-at, Completed-at-ns; _completion_record_problem is ''), set 'Status: review' in the doc and "status": "review" in board.json; sessions release --reason handoff (the released row still names the task with the real runtime); agents add --id poser --role review; work --agent poser --auto-create --type review --title 'review <task>' --scope .agent/gates/ (the same key's session file is overwritten, no row names the worker task, .git/agent-workflow/task-runtimes/ does not exist); refresh; gate approve --task <task> --by poser --rerun-tests --note self -> 'approved -> done', gate file 'Reviewer runtime: host-runtime:17fc5b9ba9c1d63cd83e7cc66bc047e3' (the runtime that ran work on the task, the same value as in 037e's note) and 'Worker runtimes: host-runtime:forged', 'Tests rerun: passed'. No access to .git/ is needed; every edit is to committed ledger files, exactly the category 037c and 037e were rejected on. The same root cause shows without any forgery: alice starts a task and releases it for handoff, bob claims and finishes it (record = bob only; cmd_start builds a fresh session dict so bob's identities never include alice), alice's key moves on to a review task and alice approves the task she started ('approved -> done'), although README promises approval 'from a conversation whose runtime never touched the change'. So README/README_CN 'the runtime check is authoritative inside one repository ... a self-review is refused there even after the committed text was edited' and the CHANGELOG's retained 'inside one repository a same-conversation self-review is refused whatever the committed text says' still state more than holds: what holds is that a runtime that FINISHED the task on this repository is refused. Fix: persist the task-keyed runtimes whenever a task-bound session is saved, not only at finish (adding _record_task_runtimes(root, task, session['runtime_identities']) after _record_runtime_identity(session) in cmd_start, tried on a temp copy of the controller, makes the no-finish scenario end in 'participated in the worker task'; doing it in _save_session for any st with a task, or archiving the session row under a task-suffixed name when its key moves on, also covers refresh/note joins and the handoff case), add a test in which the same session starts a task, hand-writes the record without finish, releases, opens a review task and is refused, and one in which the starter of a handed-off task cannot review it, and make the README/README_CN/CHANGELOG sentence say exactly what the gate can know (every runtime that worked the task on this repository, or, until then, the runtime that finished it). docs/enforcement.md still says 'differ from every runtime that participated in the worker task'; deferring it to the docs task is acceptable for one more PR (it is outside the declared write scope), and with the fix above 'participated' becomes true within one repository, which is the sentence to keep. NON-BLOCKING: _load_json returns {} for a corrupt task-runtimes file, so a hand-corrupted record is silently ignored (same by-hand boundary as rm); the record is written before the task document, so a failed write leaves an extra runtime recorded (fail-closed); the trailing-dash collision above; the gate file's Worker runtimes line exposes a value edit made on the same repository, worth a sentence in the docs. Skipped: the remaining 126 of 307 tests (nine suites, 181 distinct tests, run here; the worker's Stage Log shows its eight-suite run passing), the Windows job, and gate reconcile-github.
