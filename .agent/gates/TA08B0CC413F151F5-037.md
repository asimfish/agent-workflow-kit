# Gate TA08B0CC413F151F5-037

- Decision: approved
- By: independent-reviewer-037j
- Reviewer task: TE832D810384C4321-001
- Reviewer session started: 2026-09-09 00:29:00
- Reviewer runtime: host-runtime:7dda46a54f8fd3e4bd9d06a17091c00a
- Worker runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- At: 2026-09-09 01:23:02
- Note: APPROVE (eighth review). Confirmed on fresh temp projects installed from the branch kit at 1851e1a: reviewer 037h's exact heartbeat-joiner scenario in the no-finish shape (AGENT_WORKFLOW_SESSION_ID=w throughout: thread-a work --auto-create --done; thread-b sessions heartbeat; hand-written well-formed record with Worker-runtimes host-runtime:forged and status review; thread-b sessions release, agents add poser, work poser --type review, refresh, gate approve --rerun-tests) ends in 'reviewer host runtime participated in the worker task and is not independent', status review, no gate file, and .git/agent-workflow/task-runtimes/<task>.json lists both runtimes right after the heartbeat with <task>.lock beside it; the handoff shape (alice claims, thread-joiner heartbeats under alice's id, alice releases, bob finishes; record = alice, joiner, bob) refuses the joiner and carol approves with all three runtimes in the gate file. Race (037i): a two-process harness (each process appending 20 distinct runtimes per trial through _record_task_runtimes, start instants aligned) against the pre-fix controller 46770fd lost runtimes in 38 of 40 trials (660 missing in total); against 1851e1a 0 of 40 trials lost any of 1600 records, and 40 CLI trials of a concurrent 'sessions heartbeat' (thread-bNN) and 'refresh' (thread-cNN) under one session id all landed both runtimes with no command failing; the flock is <task>.lock next to the json, the order is always coordination lock then record lock (no reverse path, no deadlock), and the critical section is one _load_json and one atomic _save_json. Lock timeout: with the record lock held by hand for 16s a heartbeat waits 10s and dies with an uncaught TimeoutError (rc 1) after the session row was already written, so row and record disagree until the next write repairs it (confirmed: the next heartbeat recorded); the read-only hook path returned 0 in under a second throughout because it spawns the heartbeat detached and ignores its result, so no hook breaks; unreachable in normal use and inside the .git/ by-hand boundary (non-blocking below). Writer enumeration: the only _save_json on a session path is in _write_session_row; _save_session, _sessions_heartbeat and _sessions_release go through it, _clear_session only unlinks, sessions guard/list and the migration paths only read or call _save_session, and agent_workflow_hook.py writes no session rows (its one json write is the provider binding; it runs 'sessions heartbeat' as a subprocess). Over-refusal: a reviewer who heartbeat twice and noted while bound to its own review task Y is recorded under Y only and approves X; carol approves T1, then T2, rejects T3 and approves it after the worker's re-finish. Same-repository value edit refused for the worker's own conversation in every shape seen so far: 037e same session after finish, 037f no-finish hand-written record, handoff starter (alice refused after bob finished, record = alice, bob), heartbeat joiner in both shapes; no gate file in any of them. Cross-clone exactly as documented: the value-only edit committed and git-cloned is approved on the clone by the worker's own conversation (Reviewer runtime = the real worker runtime, Worker runtimes: host-runtime:forged), while a '## Notes' line inside the record ('not the last section') and a doubled lower-case header ('2 Completion Record sections') are refused there by structure and the untouched record is approved there by a fresh conversation. Reran the worker's recorded tests command by hand from the branch checkout: 71 passed in 201.81s, EXIT 0 (test_task_contract.py collects 40); multi_session_workflow, multi_session_e2e, read_receipt and multi_checkout_sync: 67 passed in 470.74s, EXIT 0; load 7-25, no flaky reruns needed; collect-only 312 matches both READMEs; kit WORKFLOW_ENTRY.md and all four _template.md equal the template copies. Docs: the CHANGELOG's review paragraphs and both README bullets match the code (the session's runtimes are the row's recorded identities, heartbeat is listed, the cross-clone limit is stated); enforcement.md's 'differ from every runtime that participated in the worker task' is now true inside one repository and deferring its cross-clone sentence to the docs task remains acceptable (outside the declared scope). NON-BLOCKING: (1) a runtime that only runs 'sessions release' on the worker's row under the same session id, never heartbeating, is not added to the row's runtime_identities and so never recorded; after a hand-written record it approves ('approved -> done', Reviewer runtime = the releaser, Worker runtimes = real + forged); live union and record agree here, so it is not the 037h inconsistency, and by the kit's own definition the releaser is a different conversation that never joined the session (no more than any fresh conversation approving a forged record, the documented boundary), but adding _record_runtime_identity(row) in _sessions_release when the releaser shares the key would make release match heartbeat; (2) _write_session_row saves the row before recording, so a record-lock timeout leaves the row ahead of the record for that write; recording first, or catching TimeoutError, would be the fail-closed order; (3) _task_runtimes_path's docstring still lists (claim, note, refresh, release, finish) without heartbeat; (4) <task>.lock files, like the json, are never deleted (62 bytes each). Skipped: the remaining 174 of 312 tests (seven suites, 138 tests run here), the Windows job, gate reconcile-github, and --rerun-tests at this gate (the planning clone runs the old controller; the command was rerun by hand from the branch checkout instead).
