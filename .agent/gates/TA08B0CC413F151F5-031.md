# Gate TA08B0CC413F151F5-031

- Decision: rejected
- By: independent-reviewer-031
- Reviewer task: T1801098AB51B5959-001
- Reviewer session started: 2026-09-05 01:41:50
- Reviewer runtime: host-runtime:1e89ff683366155480ed28797093e3d0
- Worker runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- At: 2026-09-05 02:23:47
- Note: Two defects, both verified by hand. (1) _merge_ledger_json.load() maps any non-empty unparseable or non-object side to {}, which _three_way_entries reads as 'this side deleted every task': with base holding 3 tasks, ours malformed and theirs unchanged the driver writes tasks={} and exits 0 (same for malformed theirs, or a JSON array; a malformed base resurrects archived entries), and sync would then re-render TASKS.md/PROJECT_PLAN.md from the emptied board and push - the driver must exit 1 and leave the conflict for non-empty input that does not parse to an object (only empty text is a legitimate absent side). (2) _commit_is_ledger_only treats every path under .agent/ as data, but .agent/loops/<custom>.md loop-check lines run via shell=True and .agent/loops/checkpoints.json wires them to work-start: in two clones of one bare origin a commit touching only .agent/loops/evil.md + .agent/loops/checkpoints.json with Refs: an in_progress task passed the active pre-push hook and the command executed on the other clone's next 'agentctl work'; the old rule held that until review. Restrict ledger-only (and sync's git add -A -- .agent) to an allowlist of ledger data paths (board.json, TASKS.md, PROJECT_PLAN.md, agents.json, tasks/, logs/, gates/, loops/state.json, loops/runs/, handoffs/, decisions/, bus/, archive/) and fix the docs sentence 'a commit that changes only .agent/ carries no code'.
