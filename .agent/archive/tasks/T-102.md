# T-102 - warn instead of silently overriding existing git hooks on init

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-20 15:11:48
Updated: 2026-07-20 15:11:48

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Stop init from silently breaking a mature project's own git hooks. Repointing core.hooksPath to .githooks bypasses an existing custom hooksPath (husky) or default .git/hooks scripts (black/lint/tests) with no notice. Warn clearly at init so the operator can chain them.
- Non-Goals: Auto-chaining arbitrary prior hooks; aborting install; changing merge behavior for AGENTS.md/.gitignore/CI (already correct).
- Dependencies: init hook wiring.
- Expected Deliverables: init warnings for a replaced custom hooksPath and for bypassed default hooks, failing-first regressions, docs note, no false positives on fresh/re-init.
- Definition of Done: Both cases warn; fresh install and re-init stay silent; install suite and full suite pass; independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Failing regressions for husky-style and default-hooks override warnings.
- [x] Stage 2: Warn (not abort) before repointing hooksPath over existing hooks; docs.
- [x] Stage 3: Focused/full validation, no-false-positive check, independent gate.

## Stage Log
- 2026-07-20 15:17:26 init now warns (without aborting) when repointing core.hooksPath would replace an existing custom hooksPath or bypass default .git/hooks, so a project's own pre-commit/lint/tests aren't silently lost; fresh/re-init stay silent.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: init warns (without aborting) when repointing core.hooksPath would replace an existing custom hooksPath (husky) or bypass executable default .git/hooks, so a project's own pre-commit/lint/tests are not silently lost; fresh install and re-init stay silent.
- Tests: husky+default-hook warning regressions; 5/5 boundary probes; install suite 11/11; full discover 142/142; py_compile OK; git diff --check OK; agentctl check manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-20 15:27:05
- Completed-at-ns: 1784532425623675000
