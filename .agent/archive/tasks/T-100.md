# T-100 - refuse eval run against a checkout with a live peer session

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-20 14:49:01
Updated: 2026-07-20 14:49:01

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Close an eval-harness cross-session write channel. eval run executes each case's argv in cwd=target with no session/scope check; run against a shared live checkout, a case could clobber a peer's files (verified). Refuse eval run when the target checkout has a live peer session; require an isolated baseline/candidate clone or worktree.
- Non-Goals: Sandboxing eval cases; changing the gate/signing logic; restricting eval against throwaway clones.
- Dependencies: eval run/gate flow; session store.
- Expected Deliverables: A live-peer refusal in _eval_run, a failing-first regression, docs note.
- Definition of Done: eval against a checkout with a live peer is refused and writes nothing; eval against an isolated clone still runs; eval suite and full suite pass; independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Failing regression that eval run against a live-peer checkout is refused.
- [x] Stage 2: Refuse eval run with a live peer in the target checkout; docs.
- [x] Stage 3: Focused/full validation and independent gate.

## Stage Log
- 2026-07-20 14:55:18 eval run now refuses a target checkout with a live peer session (eval cases run arbitrary argv in cwd=target and could clobber peers); isolated clones/worktrees still run.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: eval run refuses a target checkout with a live peer session (eval cases run arbitrary argv in cwd=target and could clobber peers); isolated baseline/candidate clones and worktrees still run normally.
- Tests: eval live-peer refusal regression; eval suite 3/3; full discover 140/140; py_compile OK; git diff --check OK; agentctl check manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-20 15:02:51
- Completed-at-ns: 1784530971505565000
