# T-047 - fix Windows binary signing regression mock

Status: done
Owner: codex
Agent: codex
Created: 2026-07-18 03:05:19
Updated: 2026-07-18 03:05:19

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Make the synthetic `O_BINARY` regression preserve the actual Windows binary flag when it delegates to the host `os.open`.
- Non-Goals: Change production signing behavior, weaken the synthetic flag assertion, or mask receipt-integrity failures.
- Dependencies: PR #7 Windows runs on commit `a1557c4` passed all production guidance cases but the new mock converted fixed LF bytes to CRLF.
- Expected Deliverables: A platform-aware test wrapper that records the synthetic product flag and delegates with the host binary flag.
- Definition of Done: Focused and full guidance tests pass locally and on the real Windows runner; the mutant-sensitive synthetic assertion remains intact.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tests/, .agent/`
- Files likely to touch: `tests/test_guidance_workflow.py` and T-047 workflow records.
- Files explicitly out of scope: `tools/agentctl.py`, dispatch behavior, and gate behavior.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Read the failed Windows log and distinguish a mock-induced LF-to-CRLF conversion from production receipt behavior.
- [x] Stage 2: Preserve the host binary bit in the delegated open and rerun guidance/full checks.
- [x] Stage 3: Publish and require both latest Windows runs plus Linux runs to pass.

## Stage Log

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-07-18 03:08:00 Captured the host `O_BINARY` before patching and restored it only for the wrapper's real `os.open` call; synthetic product-flag assertions remain unchanged.
- 2026-07-18 03:13:00 Focused test, all 20 guidance tests, all 90 repository tests, py_compile, diff check, and manual workflow check passed locally.
- 2026-07-18 03:25:00 Both latest PR Windows runs, both full PR workflow runs, and the post-merge main Windows/full workflow jobs passed.

## Verification

- Commands to run:
  - `python3 -m unittest tests.test_guidance_workflow.GuidanceWorkflowRegressionTest.test_signing_keys_use_binary_mode_and_preserve_exact_bytes -v`
  - `python3 -m unittest tests.test_guidance_workflow -v`
  - `python3 -m unittest discover -s tests -v`
  - `python3 -m py_compile tests/test_guidance_workflow.py`
  - `git diff --check`
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Exact binary bytes survive on all platforms while omission of the synthetic flag still fails the regression.

## Completion Record
- Summary: Fixed the cross-platform signing-key regression mock to preserve the host Windows binary flag while still asserting the product-provided synthetic flag.
- Tests: Focused binary-key test; 20 guidance tests; full 90-test suite; py_compile; git diff --check; agentctl manual check
- Worker-runtimes: host-runtime:8188dc0a82f9eda0bd147ed3c18c35eb
- Completed-at: 2026-07-18 03:13:27
- Completed-at-ns: 1784315607281606000
