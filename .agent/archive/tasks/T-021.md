# T-021 - add codex session dispatch transport

Status: done
Owner: codex
Agent: codex
Created: 2026-07-10 13:11:24
Updated: 2026-07-10 13:41:00

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Add a bounded Codex CLI transport so a supervisor can create a durable
  guidance packet and immediately dispatch it to a specific Codex session.
- Non-Goals: No daemon, scheduler, automatic merge, credential storage, or
  transport for Claude/Cursor in this task. Do not bypass Codex hook trust,
  approval, or sandbox settings.
- Dependencies: Existing session-scoped supervisor guidance from T-017 and the
  documented `codex exec resume <SESSION_ID>` interface.
- Expected Deliverables: `guidance dispatch`, `guidance create --dispatch`, a
  persisted dispatch receipt, separate model/reasoning routing, focused
  README/workflow documentation, hardened instruction read receipts, and fresh
  install regression coverage using a fake Codex executable.
- Definition of Done: Dry-run, successful dispatch, and failed dispatch paths
  are deterministic; subprocess execution does not use a shell; existing tests,
  `agentctl doctor`, and manual workflow checks pass.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, templates/project/, README.md, docs/, .agent/`
- Files likely to touch:
  - `tools/agentctl.py`
  - `tests/test_guidance_workflow.py`
  - `README.md`
  - `.agent/WORKFLOW_ENTRY.md`
  - `templates/project/.agent/WORKFLOW_ENTRY.md`
  - `.agent/tasks/T-021.md`
- Files explicitly out of scope:
  - Agent provider credentials and global Codex configuration.
  - Background services, cron definitions, and worktree pools.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Audit the current file-only guidance flow and confirm the
  official/local Codex session resume interface.
- [x] Stage 2: Implement the bounded dispatch transport and regression tests.
- [x] Stage 3: Run fresh-install and repository validation, document residual
  boundaries, and prepare the finish/review/publish handoff.
- [x] Stage 4: Resolve review rejection by separating `gpt-5.5` model selection
  from `xhigh` reasoning effort in profiles, packets, dispatch, docs, and tests.
- [x] Stage 5: Add bounded timeout evidence, source-session recursion protection,
  and safe reasoning identifier validation after final self-review.
- [x] Stage 6: Prevent `note` and `finish` from erasing stale instruction
  receipts; expand tracked workflow inputs and add a fresh-install regression.

## Stage Log

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-07-10 13:18:00 Confirmed current guidance is file-only and Codex CLI
  0.144.1 exposes `codex exec resume <SESSION_ID>`, model selection, and final
  message output for a bounded supervisor-to-worker turn.
- 2026-07-10 13:19:09 Implemented bounded codex-cli guidance dispatch with
  dry-run, persisted receipts, retry handling, and six regression tests;
  synchronized supervisor workflow documentation.
- 2026-07-10 13:36:00 Verified worker acknowledgement during an active dispatch
  does not resurrect inbox packets; removed out-of-scope files generated by the
  temporary GitNexus index before final validation.
- 2026-07-10 13:32:53 Resolved review rejection by separating Codex model and
  reasoning effort across profiles, guidance packets, dispatch commands, docs,
  and regression tests.
- 2026-07-10 13:36:51 Added timeout receipts, source-session recursion
  protection, safe reasoning identifier validation, and two dispatch regression
  tests after self-review.
- 2026-07-10 13:40:45 Hardened read receipts so workflow entry, plans, agent
  registry, rules, and checkpoint policy changes block note and finish until
  explicit refresh; added fresh-install regression coverage.

## Verification

- Commands to run:
  - `python3 -m unittest discover -s tests -v`
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py`
  - `python3 tools/agentctl.py doctor`
  - `python3 tools/agentctl.py check --mode manual`
  - `git diff --check`
- Expected result:
  - All existing tests pass and dispatch regression tests prove command
    construction, success receipts, and failure receipts without invoking a
    real Codex session.
- Results:
  - `python3 -m unittest discover -s tests -v`: passed, 15 tests.
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py`: passed.
  - `codex exec resume --model gpt-5.5 --config
    'model_reasoning_effort="xhigh"' --help`: passed argument parsing without
    starting a model turn.
  - `python3 tools/agentctl.py doctor`: passed after the final task-doc refresh.
  - `python3 tools/agentctl.py check --mode manual`: passed after removing the
    temporary GitNexus instruction injection.
  - `git diff --check`: passed.
  - GitNexus impact analysis: low risk, two direct callers (`_guidance_create`
    and `cmd_guidance`).
  - Independent `codex exec review` was attempted but stopped before review due
    to account usage limits; its startup output confirmed model `gpt-5.5` and
    reasoning effort `xhigh` are distinct runtime settings.
  - Real Codex model execution was intentionally not invoked without a separate
    target session; fresh-install tests use a fake executable and include
    acknowledgement-during-dispatch, timeout, retry, and self-recursion cases.
  - Fresh-install read-receipt regression proves instruction changes block both
    `note` and `finish` until `refresh`; loop-policy tests now refresh after
    changing checkpoint contracts.

## Completion Record
- Summary: Added bounded Fable-to-Codex session dispatch, separate model/reasoning routing, safe timeout/retry guards, durable receipts, and non-bypassable instruction read receipts.
- Tests: python3 -m unittest discover -s tests -v (15 passed); python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py; codex resume model/reasoning parser check; agentctl doctor; agentctl check --mode manual; git diff --check; template parity
- Completed-at: 2026-07-10 13:41:53
