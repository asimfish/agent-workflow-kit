# T-019 - post-merge validate guidance workflow

Status: done
Owner: codex
Agent: codex
Created: 2026-07-07 16:53:33
Updated: 2026-07-07 16:53:33

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Validate that the merged `main` branch contains the supervisor guidance
  workflow and remains installable/testable for new target projects.
- Non-Goals: Do not change implementation code, hooks, tests, or README in this
  task unless validation exposes a blocker.
- Dependencies: PR #1 has been merged into `main`.
- Expected Deliverables: Post-merge validation results recorded in this task
  document and workflow state.
- Definition of Done: `agentctl doctor`, full unittest discovery, manual check,
  and a fresh-install smoke path pass from the merged code.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/`
- Files likely to touch:
  - `.agent/tasks/T-019.md`
  - `.agent/gates/T-019.md`
  - `.agent/logs/progress.md`
- Files explicitly out of scope:
  - `tools/`
  - `tests/`
  - `hooks/`
  - `README.md`

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Confirm merged branch state and PR status.
- [x] Stage 2: Run post-merge validation commands.
- [x] Stage 3: Record results, finish, gate, commit, and push.

## Stage Log

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-07-07 16:54:24 Post-merge validation passed: doctor OK, unittest discovery passed with 9 tests, manual check OK, diff check OK, and fresh-install guidance smoke passed.

## Verification

- Commands to run:
  - `python3 tools/agentctl.py doctor`
  - `python3 -m unittest discover -s tests -v`
  - `python3 tools/agentctl.py check --mode manual`
  - `git diff --check`
  - Fresh-install smoke in a temporary Git repository.
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.
- Results:
  - `git status -sb`: `main` and `origin/main` both at merged supervisor guidance commit `771d8f1`; post-merge validation branch created from that state.
  - `gh pr view 1`: PR #1 state is `MERGED`, with successful `agent-workflow-check` CI runs.
  - `python3 tools/agentctl.py doctor`: passed.
  - `python3 -m unittest discover -s tests -v`: passed, 9 tests.
  - `python3 tools/agentctl.py check --mode manual`: passed.
  - `git diff --check`: passed.
  - Fresh-install smoke: passed in a temporary Git repository; guidance surfaced in focus, blocked manual check before ack, and passed after ack.

## Completion Record
- Summary: Validated merged supervisor guidance workflow from main with tests and fresh-install smoke.
- Tests: python3 tools/agentctl.py doctor; python3 -m unittest discover -s tests -v; python3 tools/agentctl.py check --mode manual; git diff --check; fresh-install guidance smoke
- Completed-at: 2026-07-07 16:54:49
