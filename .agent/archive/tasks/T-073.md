# T-073 - close interpreter write escapes and explicit auto-create claim

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-19 12:23:19
Updated: 2026-07-19 12:23:19

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Close the two audited gaps: (P1) interpreter/script/stream shell writes bypass the mutating classifier so they skip session guards entirely; (P2) `work --auto-create --new-id/--title` claims an unrelated queued task for the same agent name instead of creating the requested task.
- Non-Goals: Static path extraction from arbitrary code (undecidable); sandboxing; changing the documented worktree requirement for parallel Git.
- Dependencies: Merged document-ownership release (PR #11/#12).
- Expected Deliverables: (1) mutating classification for interpreter/inline-code/stream/script invocations with existing read-only patterns preserved; (2) a working-tree contamination check in `sessions guard` that blocks when tracked files are modified outside every live session scope; (3) explicit auto-create requests bypass queue selection; (4) failing-first regressions for all three.
- Definition of Done: New regressions red then green, hook read-only passthroughs unchanged, full suite green, independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Failing regressions for interpreter-write bypass, contamination detection, and explicit auto-create claim.
- [x] Stage 2: Extend the mutating classifier, add the contamination guard, and reorder explicit auto-create before queue selection.
- [ ] Stage 3: Focused + full validation and independent gate.

## Incident Note

- 2026-07-19 12:2x: While purging iCloud " 2" duplicate files, the worker accidentally deleted the untracked T-073 task doc and two of today's loop triage run reports; the task doc was rebuilt from the template immediately, board state was unaffected, loop evidence regenerates on the next cycles.

## Stage Log
- 2026-07-19 20:58:04 Rebuilt the T-073 task doc after an accidental delete during iCloud-duplicate cleanup; recorded the audited P1/P2 contract.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Closed both audited gaps: opaque shell writers (interpreters, scripts, rsync, archive/download outputs) are now mutating and gated, escaped writes are caught by working-tree reconciliation against all live scopes, and explicit auto-create requests always claim the named task instead of an agent-name queue match.
- Tests: 3 new regressions red->green; tests.test_multi_session_workflow 22/22; ownership+e2e+install+migrate 27/27; full discover 122/122; py_compile OK; git diff --check OK; agentctl check --mode manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-19 21:28:06
- Completed-at-ns: 1784467686644697000
