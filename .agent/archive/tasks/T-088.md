# T-088 - block ancestor and glob paths that exceed task scope

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-20 07:01:51
Updated: 2026-07-20 07:01:51

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Close a self-audit scope-containment hole: a write target that is an ANCESTOR of the task scope (e.g. `src` or `src/*` when scoped to `src/one/`) reaches sibling scopes but was accepted because _scopes_overlap treats ancestors and descendants symmetrically. Make single-path scope membership directional and glob-aware.
- Non-Goals: Changing _scopes_overlap (used for symmetric disjointness proofs between two task scopes); expanding globs at runtime.
- Dependencies: T-081 shell parsing.
- Expected Deliverables: Directional _path_in_scope that rejects ancestors and only admits globs whose expansion directory is inside scope, a failing-first regression, docs note.
- Definition of Done: `rm src/*`, `rm src`, bare `*` blocked from a src/one session; own descendants and same-dir globs allowed; suites pass; independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Failing regression for ancestor/glob scope escapes.
- [x] Stage 2: Directional, glob-aware _path_in_scope; keep _scopes_overlap for disjointness.
- [x] Stage 3: Focused/full validation and independent gate (also deflaked the launch-handshake timing that raced heavy load).

## Stage Log
- 2026-07-20 07:17:57 Made single-path scope membership directional and glob-aware: ancestor paths (src) and cross-scope globs (src/*) that exceed the task scope are now rejected; _scopes_overlap unchanged for symmetric disjointness.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Single-path scope membership is directional and glob-aware: ancestor paths (src) and sibling-spanning globs (src/*, bare *) are rejected while own descendants and same-directory globs stay allowed; deflaked the pending-launch handshake test.
- Tests: ancestor/glob regression red->green; guard attack matrix 12/12 blocked + 5/5 allowed; focused 60/60; loop 31/31; full discover 133/133; py_compile OK; git diff --check OK; agentctl check manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-20 12:03:35
- Completed-at-ns: 1784520215368636000
