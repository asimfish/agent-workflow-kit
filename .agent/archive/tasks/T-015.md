# T-015 - add pre-push new branch range regression

Status: done
Owner: codex
Agent: codex
Created: 2026-07-06 17:48:40
Updated: 2026-07-06 17:48:40

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Add regression coverage for the first-push feature-branch pre-push
  range so old mainline commits are not revalidated.
- Non-Goals: Do not change hook behavior in this task; T-014 already patched it.
- Dependencies: `hooks/pre-push` / `.githooks/pre-push` range behavior from T-014.
- Expected Deliverables: A test that simulates Git's pre-push stdin for a new
  remote branch and verifies the hook passes `merge-base..HEAD` to `agentctl`.
- Definition of Done: The new regression test passes, full test discovery
  passes, and workflow checks pass.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tests/, .agent/`
- Files likely to touch:
  - `tests/test_pre_push_hook.py`
  - `.agent/tasks/T-015.md`
- Files explicitly out of scope:
  - `hooks/pre-push`
  - `.githooks/pre-push`
  - `tools/agentctl.py`

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Add focused hook-range regression test.
- [x] Stage 2: Run targeted and full test commands.
- [x] Stage 3: Finish, gate, commit, and push.

## Stage Log

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-07-06 17:50:06 Added pre-push hook range regression covering first new-branch push and existing-branch push; targeted and full unittest runs pass.

## Verification

- Commands to run:
  - `python3 -m unittest tests.test_pre_push_hook -v`
  - `python3 -m unittest discover -s tests -v`
  - `python3 tools/agentctl.py check --mode manual`
  - `git diff --check`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.
- Results:
  - `python3 -m unittest tests.test_pre_push_hook -v`: passed, 2 tests.
  - `python3 -m unittest discover -s tests -v`: passed, 7 tests.
  - `python3 tools/agentctl.py check --mode manual`: passed after refresh.
  - `git diff --check`: passed.

## Completion Record
- Summary: Added regression coverage for first-push pre-push hook range.
- Tests: python3 -m unittest tests.test_pre_push_hook -v; python3 -m unittest discover -s tests -v; python3 tools/agentctl.py check --mode manual; git diff --check
- Completed-at: 2026-07-06 17:50:26
