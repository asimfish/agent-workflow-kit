# T17F063E6115138DE-004 - exclude already-published commits from pre-push validation

Status: done
Owner: codex
Agent: codex
Created: 2026-07-20 13:51:04
Updated: 2026-07-20 13:51:04

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Make pre-push validation ignore commits already reachable from another
  branch on the same remote while continuing to validate every unpublished
  commit introduced by the push.
- Non-Goals: Do not weaken commit, task, documentation, or gate policy; do not
  exclude commits reachable only from local branches or from a different
  remote.
- Dependencies: The failed ordinary push of `e801c7e` and diagnostic task
  `T17F063E6115138DE-003` provide the reproduction and identify
  `hooks/pre-push` as canonical.
- Expected Deliverables: A canonical/mirrored hook fix, focused regression
  coverage for existing and new branches plus a merged remote-main history,
  and passing full workflow tests.
- Definition of Done: An ordinary push of this branch no longer rejects the
  non-Conventional merge commit already published on `origin/main`; an
  unpublished non-Conventional commit is still rejected; source/template and
  hook/mirror parity checks pass. The implementation task verifies the exact
  branch range through the installed hook; the immediately following release
  task records the network push because in-progress tasks are intentionally
  not pushable.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/, hooks/, .githooks/, tools/, templates/project/, tests/`
- Files likely to touch: `hooks/pre-push`, `.githooks/pre-push`,
  `tools/agentctl.py`, and `tests/test_pre_push_hook.py`.
- Files explicitly out of scope: Product features unrelated to pre-push commit
  selection and all remote history rewriting.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Specify the remote-reachability filter and regression cases.
- [x] Stage 2: Implement canonical/mirror tooling changes with fail-closed input handling.
- [x] Stage 3: Run focused, full, workflow, parity, and exact-branch hook verification.

## Stage Log
- 2026-07-20 13:56:26 Implemented same-remote published-commit filtering and added focused Git-graph regression coverage; 3/3 focused tests pass.
- 2026-07-20 14:15:00 Verified the exact rejected branch range: unfiltered check reproduces the origin/main merge false positive and the installed same-remote filter passes.
- 2026-07-20 14:15:00 Final tree passed focused 3/3, full 149/149, manual, doctor, py_compile, bash syntax, hook parity, JSON, and diff checks.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 -m unittest tests.test_pre_push_hook`
  - `python3 -m unittest discover -s tests -p 'test_*.py'`
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 -m py_compile tools/agentctl.py`
  - `cmp hooks/pre-push .githooks/pre-push`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Exclude commits already reachable from the configured push remote while preserving fail-closed validation for unpublished and other-remote commits.
- Tests: focused 3/3; full 149/149 twice; exact branch range unfiltered FAIL and filtered OK; manual/doctor/py_compile/bash-n/cmp/json/diff checks OK
- Worker-runtimes: host-runtime:8188dc0a82f9eda0bd147ed3c18c35eb
- Completed-at: 2026-07-20 14:15:20
- Completed-at-ns: 1784528120819870000
