# T-104 - make checkpoint debounce change-aware per its documented contract

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-20 16:25:49
Updated: 2026-07-20 16:25:49

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Fix a loop contract-vs-implementation mismatch found by analyzing the loop system: the daily-plan-triage contract says "do not rerun ... unless the plan changed", but checkpoint debounce was time-only, so a genuinely changed plan within the 30m window was silently skipped. Make debounce change-aware.
- Non-Goals: Adding a scheduler/daemon; changing loop execution or escalation; per-loop input fingerprints (coordination-doc fingerprint suffices for the built-ins).
- Dependencies: _run_loop_checkpoint_unlocked, _checkpoint_recent.
- Expected Deliverables: A coordination-doc fingerprint (plan/tasks/board) recorded per checkpoint run; debounce skips only when recent AND unchanged; a failing-first regression; README note.
- Definition of Done: Unchanged reruns still debounce; a plan/tasks/board change bypasses the window; --force still bypasses; work-start auto-trigger and loop suite unaffected; independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Failing regression that a plan change within the debounce window is skipped.
- [x] Stage 2: Change-aware debounce via a coordination-doc fingerprint; README note.
- [x] Stage 3: Focused/full validation and independent gate.

## Stage Log
- 2026-07-20 16:37:18 Checkpoint debounce is now change-aware: a plan/tasks/board change bypasses the time window (matching the 'unless the plan changed' contract); unchanged reruns still debounce, --force still bypasses.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Checkpoint debounce is change-aware: within the window a rerun is skipped only when PROJECT_PLAN.md/TASKS.md/board.json are unchanged since the last run; a genuine change bypasses the window per the 'unless the plan changed' contract, --force still bypasses unconditionally.
- Tests: change-aware debounce regression red->green; loop+multi-session 68/68; full discover 143/143; py_compile OK; git diff --check OK; agentctl check manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-20 17:05:11
- Completed-at-ns: 1784538311370609000
