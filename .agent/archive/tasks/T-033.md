# T-033 - reconcile merged GitHub reviews

Status: done
Owner: supervisor
Agent: supervisor
Created: 2026-07-16 19:36:15
Updated: 2026-08-27 15:53:05

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Close the durable task lifecycle after a human merges a GitHub PR so
  merged work does not remain indefinitely in `review`.
- Non-Goals: Automatically merge PRs, bypass branch protection, or treat an
  unverified local claim as human approval.
- Dependencies: Independent agent gates from T-029 through T-032 and GitHub CLI
  access to authoritative PR merge metadata.
- Expected Deliverables: A fail-closed `gate reconcile-github` action, audit
  records, regressions, documentation, and reconciliation of PR #5 tasks.
- Definition of Done: Reconciliation accepts only a merged PR whose merge commit
  is in current history, whose mergedBy matches `--by`, and whose file list
  contains the task document; negative cases fail without changing task state.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, README.md, docs/, templates/project/, .agent/`
- Files likely to touch: `tools/agentctl.py`,
  `tests/test_install_and_hooks.py`, README/enforcement docs, and `.agent/`
  reconciliation evidence.
- Files explicitly out of scope: dispatch transport, loop execution, and
  automatic GitHub merging.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Identify the post-merge state gap from PR #5 and define the
  external-evidence trust boundary.
- [x] Stage 2: Implement fail-closed GitHub merge reconciliation and regression
  coverage.
- [x] Stage 3: Reconcile T-029 through T-032, run full verification, and publish
  the post-merge state repair.

## Stage Log
- 2026-07-16 19:41:09 Implemented fail-closed and idempotent GitHub merge reconciliation; focused negative/positive regression passes and PR #5 evidence reconciled T-029 through T-032 to done.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 tools/agentctl.py doctor`
  - `python3 -m unittest discover -s tests -q`
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py`
  - `git diff --check`
- Expected result:
  - All 78 regressions and workflow checks pass; merged PR evidence reconciles
    exactly the covered review tasks and fails closed otherwise.

## Completion Record
- Summary: Added authoritative GitHub merge reconciliation and closed the stale PR #5 review tasks with auditable evidence.
- Tests: 78 unittest regressions; doctor; manual check; py_compile; git diff --check
- Worker-runtimes: host-runtime:8188dc0a82f9eda0bd147ed3c18c35eb
- Completed-at: 2026-07-16 19:43:25
- Completed-at-ns: 1784202205438011000

## Administrative Closure (2026-08-27 15:53:05)

- Closed by supervisor during board hygiene (TR review task, 2026-08-27): the underlying work was merged long ago; these entries predate the independent-gate record format, so no gate file exists to auto-close them.
