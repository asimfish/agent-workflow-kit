# TA08B0CC413F151F5-019 - mirror registration grace windows in force-stale release and rejection hints

Status: review
Owner: cursor
Agent: cursor
Created: 2026-08-26 21:36:32
Updated: 2026-08-26 21:36:32

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: close the reviewer follow-up from TA08B0CC413F151F5-018 — a missing holder inside the acquire-before-register window could be force-stale released and the acquire rejection hint advertised that command; mirror the orphan-sweep grace windows (run 10 min, conversation 1 h) in `_resource_release_stale` and gate the rejection hint on provably-dead holders only.
- Non-Goals: changing sweep behavior, liveness classification, or lock ownership validation (owner.json identity check is a separate follow-up).
- Dependencies: TA08B0CC413F151F5-018 (merged, PR #45).
- Expected Deliverables: `_missing_holder_grace_expired` helper wired into force-stale and `_acquire_rejection_detail`; docs note on the grace window and the accepted check-then-release window; regression updates.
- Definition of Done: full suite green; force-stale refuses a grace-window missing holder and frees an aged one; rejection hint absent for grace-window missing holders.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, docs/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: implement grace-window guard in force-stale and rejection hints, update docs
- [x] Stage 2: regression updates (grace-window refusal, aged release, hint gating)
- [ ] Stage 3: full-suite verification, independent review, PR, CI, merge

## Stage Log

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- No updates yet.

## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Closed the TA08B0CC413F151F5-018 reviewer follow-up: force-stale now refuses missing holders inside the registration grace windows (run 10 min, conversation 1 h, mirroring the orphan sweep) via _missing_holder_grace_expired, the acquire rejection only advertises --force-stale for provably dead holders, and docs state the grace window plus the accepted non-atomic check-then-release window.
- Tests: python3 -m pytest tests/ -q -> 204 passed (0:11:25); updated force-stale regression covers grace-window refusal then aged release; acquire diagnostics regression pins that a grace-window missing holder gets no --force-stale hint
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-08-26 22:23:18
- Completed-at-ns: 1787754198460494000
