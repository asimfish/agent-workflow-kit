# TA08B0CC413F151F5-023 - pave the worktree finish-to-gate path: merge-back tooling and doc (B-2)

Status: todo
Owner: unassigned
Agent: unassigned
Created: 2026-08-28 01:25:30
Updated: 2026-08-28 01:25:30

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal:
- Non-Goals:
- Dependencies:
- Expected Deliverables:
- Definition of Done:

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, docs/, tests/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1:
- [ ] Stage 2:
- [ ] Stage 3:

## Stage Log

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- No updates yet.

## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record

- Summary:
- Tests: not run
- Artifacts: none
- Follow-ups: none

## Scope (from final acceptance, finding B-2 moderate + rough edges)

Worktree tasks strand their completion/review evidence on the feature branch:
nothing prompts committing the finish record, the planning-checkout gate then
fails with misleading errors, and the manual merge conflicts in board.json,
TASKS.md, PROJECT_PLAN.md, loops/state.json for every parallel worktree.

- Add a `reconcile`/`worktree` assist that commits the .agent finish record in
  the worktree and merges/unions the ledger back to the planning checkout
  (JSON-aware union for board.json, row-aware for TASKS.md).
- Fix the misleading gate error ("no host runtime evidence" when the real
  problem is an unsynced ledger) to point at the sync step.
- Write docs/worktree-merge-back.md walking the finish -> gate -> merge path.
- B-3 (low): correct the worktree/exclusive refusal hint (bare auto-select
  requirement + task doc committed at HEAD).
- B-1 (low): enforce the documented request-id intent check even while the
  owning session is still active (warn or error on intent mismatch instead of
  silently resuming).
