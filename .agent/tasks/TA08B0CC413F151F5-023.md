# TA08B0CC413F151F5-023 - pave the worktree finish-to-gate path: merge-back tooling and doc (B-2)

Status: done
Owner: unassigned
Agent: unassigned
Created: 2026-08-28 01:25:30
Updated: 2026-08-28 01:25:30

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: tool the worktree finish-to-gate path (ledger merge-back) and fix the
  misleading refusals found during final acceptance (B-1, B-3, gate errors),
  plus two view/check defects found while dogfooding PRs #49/#50.
- Non-Goals: automating `git merge` of code changes; README updates (outside
  the task scope; follow-up docs task).
- Dependencies: none.
- Expected Deliverables: `agentctl reconcile merge-back` subcommand; corrected
  gate/work refusal messages; B-1 intent guard; `_plan_task_rows` multi-hyphen
  id fix; pre-push archive lookup; regression tests; updated
  `docs/worktree-merge-back.md`.
- Definition of Done: full test suite passes; independent review approves;
  the ledger of this very task reaches the planning checkout via the new
  merge-back subcommand (dogfood).

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, docs/, tests/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: implement `reconcile merge-back` (per-task ledger import from a
  git ref, fail-closed: session required, planning checkout only, id-shape
  validated, never regresses local status, dry-run supported)
- [x] Stage 2: fix the four misleading/incorrect behaviors -- B-1 explicit
  create must not silently resume other work; B-3 worktree refusal now names
  the bare `work` invocation; gate errors for missing task / missing runtime
  evidence now point at merge-back; `_plan_task_rows` accepts multi-hyphen
  ids; pre-push resolves task refs against `.agent/archive/board.json`
- [x] Stage 3: regression tests (`tests/test_merge_back_workflow.py`, 7 cases)
  and `docs/worktree-merge-back.md` rewritten around the subcommand

## Stage Log

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-08-28 21:55:00 implemented merge-back + 4 fixes; new test file passes
  (7/7); full suite run in progress. Note: the two "rough edge" defects
  (plan-row regex, pre-push archive) were recorded on the planning branch
  copy of this task doc after this worktree branched; they are in scope and
  fixed here.
- 2026-08-29 00:55:00 full suite: 213 passed, 2 failed. (1) identity-policy
  leaf coverage: real gap, the new (reconcile, merge-back) leaf was not
  registered in the expected set; fixed, test passes. Default policy for the
  leaf is correct (identity required, not allowed during upgrade drain).
  (2) test_gpu_watchdog_fails_safe_for_remote_termination_and_probe_errors
  fails identically on a clean main checkout (983fbd9) on this macOS host
  ('active' != 'probe_error' timing); pre-existing environment failure, not
  a regression from this change; CI (Linux) passes it on PR #49/#50.

## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Added 'agentctl reconcile merge-back' (per-task ledger import from a git ref: board entry, task doc, gate record, re-rendered views; fail-closed on session/checkout/id-shape; never regresses local status; dry-run). Fixed B-1 (explicit create refuses to silently resume other work), B-3 (worktree refusal names the bare work invocation), gate errors now point at merge-back when the ledger is unsynced, _plan_task_rows accepts multi-hyphen ids, pre-push resolves refs against .agent/archive/board.json. Rewrote docs/worktree-merge-back.md around the subcommand.
- Tests: tests/test_merge_back_workflow.py 7/7; full suite 213 passed / 2 failed -> identity-policy leaf gap fixed (now passes), gpu-watchdog probe_error failure reproduced identically on clean main 983fbd9 (pre-existing macOS timing, CI green on #49/#50)
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-08-29 00:55:25
- Completed-at-ns: 1787936125827842000
