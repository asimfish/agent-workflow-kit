# T-031 - fix Windows dispatch identity and decoding

Status: done
Owner: codex
Agent: codex
Created: 2026-07-15 21:03:43
Updated: 2026-07-15 21:03:43

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Restore deterministic Windows guidance receipt verification and make
  the dispatch regression fixture tolerant of Windows console encodings.
- Non-Goals: Change guidance semantics, add a new transport, or expand Fable
  execution capabilities.
- Dependencies: T-026 guidance acceptance and the Windows CI coverage from
  T-028.
- Expected Deliverables: Canonical persisted-value receipt signing, a
  Windows-safe fake Codex subprocess fixture, and complete worker-runtime
  evidence when different runtimes start and finish a task, plus newline-stable
  managed-install diagnostics.
- Definition of Done: The previously failing worktree acceptance regression,
  the complete guidance suite, and all repository checks pass locally and in
  GitHub Actions.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, templates/project/, docs/, README.md, .agent/`
- Files likely to touch: `tools/agentctl.py`,
  `tests/test_guidance_workflow.py`, and T-031 workflow evidence.
- Files explicitly out of scope: guidance routing behavior and documentation
  unrelated to this cross-platform defect.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Reproduce the Windows CI failure from its captured receipt and
  identify signing/decoding platform boundaries.
- [x] Stage 2: Canonicalize receipt values before signing and harden the Windows
  fake Codex fixture's nested subprocess decoding.
- [x] Stage 2b: Persist every runtime that records progress or finalizes a task
  so a finishing runtime cannot switch roles and self-approve.
- [x] Stage 2c: Use the installer's normalized text hashing in doctor so CRLF
  managed files are not reported as drifted.
- [x] Stage 3: Run focused and full verification, then push the repair through
  PR #5 CI.

## Stage Log
- 2026-07-15 21:09:20 Focused fixes now pass: 5 install/gate tests and 19 guidance tests; independent review findings P1/P2 are covered by regressions.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 tools/agentctl.py doctor`
  - `python3 -m unittest discover -s tests -v`
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py`
  - `git diff --check`
- Expected result:
  - Workflow checks and all 77 regressions pass, including Windows guidance,
    cross-runtime gate, and CRLF managed-file cases.

## Completion Record
- Summary: Fixed Windows guidance receipt stability, captured cross-runtime finalizers as worker participants, and aligned doctor hashing with CRLF-normalized installation manifests.
- Tests: 77 unittest regressions; doctor; manual check; py_compile; git diff --check
- Worker-runtimes: host-runtime:8188dc0a82f9eda0bd147ed3c18c35eb
- Completed-at: 2026-07-15 21:11:16
- Completed-at-ns: 1784121076972639000
