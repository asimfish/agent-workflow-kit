# T-010 - add repeatable loop regression test

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-03 18:25:39
Updated: 2026-07-03 18:25:39

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: turn the T-008 one-off dogfood scenario into a repeatable regression test that exercises the loop feedback chain (custom loop failure -> follow-up packet -> escalation -> check/finish blocking -> fix auto-close -> ack override) in a fresh installed project, and run it in CI.
- Non-Goals: no new runtime features, no daemon/scheduler, no third-party test dependencies.
- Dependencies: T-005 follow-up packets, T-006 custom loop execution, T-007 escalation/ack, T-008 dogfood evidence.
- Expected Deliverables: `tests/test_loop_workflow.py` (stdlib unittest), CI step running the tests, README pointer to the regression tests.
- Definition of Done: the test passes locally from a clean checkout via `python3 -m unittest discover -s tests`, covers both the fail/escalate/auto-close path and the ack override path in fresh temp projects, and the GitHub workflow runs it.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tests/, .agent/, .github/, README.md`
- Files likely to touch: `tests/test_loop_workflow.py`, `.github/workflows/agent-workflow-check.yml`, `README.md`, `.agent/tasks/T-010.md`
- Files explicitly out of scope: `tools/agentctl.py` runtime behavior, templates content, hooks

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Write stdlib unittest regression covering install + fail/escalate/block/fix/auto-close and ack override in fresh temp projects.
- [x] Stage 2: Wire the test into the GitHub workflow and document it in README.
- [ ] Stage 3: Run full verification (unittest, check --mode manual, py_compile, git diff --check) and finish.

## Stage Log
- 2026-07-03 18:33:07 Added tests/test_loop_workflow.py (stdlib unittest, fresh temp-project install) covering fail/escalate/block/fix/auto-close and ack override; wired into CI workflow and documented in README; both tests pass locally.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 -m unittest discover -s tests -v`
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py tests/test_loop_workflow.py`
  - `git diff --check`
- Expected result:
  - Both regression tests pass in fresh temp projects; workflow checks pass.

## Completion Record
- Summary: Turned the T-008 one-off dogfood into a repeatable stdlib unittest regression that installs the kit into fresh temp Git projects and replays the loop feedback chain (fail -> follow-up -> escalate -> check/finish block -> fix auto-close; ack override), wired it into the CI workflow, and documented it in README.
- Tests: python3 -m unittest discover -s tests (2 passed); py_compile agentctl/hook/tests; agentctl check --mode manual; git diff --check
- Completed-at: 2026-07-03 18:33:31
