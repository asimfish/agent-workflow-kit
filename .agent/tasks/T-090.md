# T-090 - block symlink and hardlink creation aliasing peer scopes

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-20 12:22:24
Updated: 2026-07-20 12:22:24

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Close a self-audit symlink-aliasing gap: creating a link whose target resolves outside the task scope (e.g. `ln -s ../peer own/link`) was allowed because only the link node was scope-checked, letting a link alias a peer's tree. Claim the link target too, resolving symlink targets relative to the link directory and hard-link targets relative to cwd. Also confirmed the efficiency side: solo sessions and own-scope work beside peers are never over-blocked.
- Non-Goals: Runtime filesystem sandboxing; blocking links whose target stays inside scope.
- Dependencies: T-088 directional scope containment.
- Expected Deliverables: ln target extraction with correct relative base, a failing-first regression, and an efficiency check documenting no solo/own-scope over-block.
- Definition of Done: Peer/absolute/hardlink-to-peer link creation blocked, own-scope links allowed, solo work unaffected, suites pass, independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Failing regression for symlink/hardlink targets aliasing a peer.
- [x] Stage 2: Extract and scope-check link targets with the correct relative base.
- [ ] Stage 3: Focused/full validation, efficiency audit, independent gate.

## Stage Log
- 2026-07-20 12:22:25 Symlink/hardlink creation now scope-checks the link target (symlink target relative to link dir, hardlink relative to cwd); efficiency audit confirmed solo sessions and own-scope work beside peers are never over-blocked.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Symlink/hardlink creation now scope-checks the link target with correct relative base; solo and own-scope efficiency preserved.
- Tests: symlink regression red->green; focused 61/61; full discover 134/134; py_compile OK; git diff --check OK; agentctl check manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-20 12:33:52
- Completed-at-ns: 1784522032606635000
