# T-090 - close T-089 hook-shell runtime fingerprint skew

Status: in_progress
Owner: codex
Agent: codex
Created: 2026-07-20 07:23:52
Updated: 2026-07-20 07:23:52

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Recover the active provider shell session when hook and shell processes
  expose different subsets of runtime identity variables, while preserving
  strict isolation between provider payloads, checkouts, and forks.
- Non-Goals: Redefine the global controller runtime fingerprint; weaken opaque
  command guards; infer across ambiguous candidate sessions; add provider-specific
  dispatch behavior.
- Dependencies: T-089 independently reproduced and rejected T-088 because the
  hook saw the outer runtime while Claude Bash added `CLAUDE_CODE_SESSION_ID`.
- Expected Deliverables: A bounded provider-shell candidate resolver, persisted
  bound session key, fail-closed ambiguity handling, source/template parity,
  skewed-environment regressions, and corrected boundary documentation.
- Definition of Done: A fresh real Claude CLI can create a task, run opaque test
  commands over multiple turns, record notes, gate, finish, commit, and push;
  competing payloads remain blocked; ambiguous, corrupt, fork, propagated-ID,
  and worktree paths remain isolated; focused/install/full/static checks pass;
  a new independent runtime approves the resubmitted task before merge.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/, tests/, README.md, docs/, .agent/`
- Files likely to touch: `tools/agent_workflow_hook.py`,
  `templates/project/tools/agent_workflow_hook.py`,
  `tests/test_multi_session_workflow.py`, `README.md`, `docs/enforcement.md`, and
  this task's `.agent/` records.
- Files explicitly out of scope: provider executables, external user settings,
  controller runtime identity semantics, model dispatch, and application code.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Encode T-089's hook/shell runtime skew as failing-first tests,
  including an ambiguous derived-session case.
- [x] Stage 2: Resolve and persist one unambiguous provider-derived shell
  session, fail closed otherwise, and update source/template/docs.
- [ ] Stage 3: Run focused/install/full/static/fresh-install and real nested-CLI
  verification, then obtain a fresh independent gate before merge.

## Stage Log
- 2026-07-20 07:57:43 Fresh-install dogfood passed in a new Git repository: the outer hook runtime recovered the task created by a shell that added CLAUDE_CODE_SESSION_ID, then allowed a native write, a later note, and an opaque unittest command under the same persisted anonymous session key.
- 2026-07-20 07:41:53 Implemented bounded provider-shell session discovery with unique anonymous-key pinning and explicit ambiguity rejection; source/template parity, py_compile, diff-check, focused tests, and all 41 multi-session regressions pass. Added failing-first and fixed gate empty-note whitespace regression.
- 2026-07-20 07:26:51 Failing-first runtime-skew regressions confirmed: a shell session created after adding CLAUDE_CODE_SESSION_ID is invisible to the hook, and two derived shell sessions produce only a generic no-session block instead of an explicit ambiguity failure.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-07-20 07:24:30 T-089 evidence integrated: hook runtime
  `8188dc0a...` and Claude shell runtime `5197e358...` differ solely because the
  shell adds `CLAUDE_CODE_SESSION_ID`; T-088 is blocked pending this fix.

## Verification

- Commands to run:
  - `python3 -m unittest tests.test_multi_session_workflow`
  - `python3 -m unittest tests.test_install_and_hooks`
  - `python3 -m unittest discover -s tests -p 'test_*.py'`
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record

- Summary:
- Tests: not run
- Artifacts: none
- Follow-ups: none
