# TA08B0CC413F151F5-007 - retention pruning for terminal run leases and artifacts

Status: done
Owner: cursor
Agent: cursor
Created: 2026-08-12 06:05:52
Updated: 2026-08-12 06:05:52

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal:
- Non-Goals:
- Dependencies:
- Expected Deliverables:
- Definition of Done:

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, docs/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1:
- [ ] Stage 2:
- [ ] Stage 3:

## Stage Log
- 2026-08-12 06:10:06 Added retention pruning: run start opportunistically removes terminal run leases (succeeded/failed/cancelled), released resource leases, their per-run artifacts, and aged orphan run files once older than run_artifact_retention_days (runtime policy, default 14, 0 disables). Non-terminal and recent leases plus release_failed resources are never touched; pruning is lock-timeout tolerant and can never block a start. Regression covers pruned/kept/orphan/disabled matrices.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Closed the log-accumulation residual: terminal run leases, released resource leases, per-run artifacts, and aged orphan run files now age out via an opportunistic, fail-open prune in run start, governed by run_artifact_retention_days in the runtime policy (default 14 days, 0 disables). Live, non-terminal, recent, and release_failed state is never pruned.
- Tests: new retention regression OK (0.4s); execution-lease file 18/18 in 66.6s; py_compile OK
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-08-12 06:10:06
- Completed-at-ns: 1786486206938426000
