# T-014 - fix pre-push range for first feature branch push

Status: done
Owner: codex
Agent: codex
Created: 2026-07-06 01:28:41
Updated: 2026-07-06 01:28:41

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Fix the pre-push hook so the first push of a feature branch checks only
  commits introduced after the mainline merge base.
- Non-Goals: Do not weaken commit/task validation or bypass hooks.
- Dependencies: Existing `agentctl check --mode pre-push` and adoption baseline
  behavior.
- Expected Deliverables:
  - `.githooks/pre-push` and source `hooks/pre-push` compute a bounded range
    for new remote branches.
  - The fix keeps normal existing-branch push behavior unchanged.
- Definition of Done: Manual pre-push checks pass for `main..HEAD`, hook syntax
  is valid, and workflow checks pass.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `hooks/pre-push, .githooks/pre-push, .agent/`
- Files likely to touch:
  - `hooks/pre-push`
  - `.githooks/pre-push`
  - `.agent/tasks/T-014.md`
- Files explicitly out of scope:
  - `tools/agentctl.py`
  - T-013 guidance implementation files.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Patch new-branch range calculation.
- [x] Stage 2: Verify hook syntax and pre-push check range.
- [ ] Stage 3: Finish, gate, commit, and push branch.

## Stage Log
- 2026-07-06 01:30:16 Fixed pre-push new-branch range to use the remote main/master merge base and verified hook syntax plus simulated first branch push.
- 2026-07-06 01:29:45 Fixed pre-push new-branch range calculation and verified hook syntax plus simulated first feature-branch push.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `bash -n hooks/pre-push && bash -n .githooks/pre-push`
  - `python3 tools/agentctl.py check --mode pre-push --commit-range "$(git merge-base HEAD origin/main)..HEAD"`
  - Simulated first feature-branch push into `.githooks/pre-push` with remote SHA all zero.
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.
  - New-branch hook simulation checks only branch commits and passes.

## Completion Record
- Summary: Fixed first-push pre-push hook range for new feature branches.
- Tests: bash -n hooks/pre-push && bash -n .githooks/pre-push; python3 tools/agentctl.py check --mode pre-push --commit-range 685c3e40bbb831477a40d31f928a70b306710ece..HEAD; simulated first feature-branch pre-push; python3 tools/agentctl.py check --mode manual; git diff --check
- Completed-at: 2026-07-06 01:30:00
