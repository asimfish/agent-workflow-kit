# T-032 - prevent completion evidence injection

Status: done
Owner: codex
Agent: codex
Created: 2026-07-15 21:18:07
Updated: 2026-07-15 21:18:07

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Prevent worker-controlled completion text from injecting trusted gate
  evidence fields.
- Non-Goals: Replace Markdown task documents with a new storage format or
  change runtime fingerprint semantics.
- Dependencies: T-030 independent runtime gates and T-031 cross-runtime
  finalization evidence.
- Expected Deliverables: Single-line completion field encoding and an
  adversarial multiline summary/tests regression.
- Definition of Done: Injected `Worker-runtimes` and heading text remains inside
  its original field, the genuine runtime line is unique, and all checks pass.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, .agent/`
- Files likely to touch: `tools/agentctl.py`,
  `tests/test_install_and_hooks.py`, and T-032 workflow evidence.
- Files explicitly out of scope: installation, dispatch, and loop behavior.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Reproduce the completion-record field injection found by clean
  independent review.
- [x] Stage 2: Collapse worker-provided summary and test evidence to single-line
  Markdown field values and add adversarial regression coverage.
- [x] Stage 3: Run focused and full verification, then re-run clean review and
  remote CI.

## Stage Log
- 2026-07-15 21:18:40 Adversarial multiline completion evidence is now encoded into single-line fields; forged Worker-runtimes and headings cannot create trusted fields, and focused gate regression passes.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 tools/agentctl.py doctor`
  - `python3 -m unittest discover -s tests -q`
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py`
  - `git diff --check`
- Expected result:
  - Workflow checks and all 77 regressions pass; injected completion text cannot
    create additional trusted evidence fields.

## Completion Record
- Summary: Prevented multiline completion fields from injecting trusted worker runtime evidence.
- Tests: 77 unittest regressions; doctor; manual check; py_compile; git diff --check
- Worker-runtimes: host-runtime:8188dc0a82f9eda0bd147ed3c18c35eb
- Completed-at: 2026-07-15 21:20:28
- Completed-at-ns: 1784121628352442000
