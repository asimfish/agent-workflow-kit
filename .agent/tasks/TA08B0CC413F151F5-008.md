# TA08B0CC413F151F5-008 - release resources orphaned by finished runs

Status: review
Owner: cursor
Agent: cursor
Created: 2026-08-12 06:38:06
Updated: 2026-08-12 06:38:06

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal:
- Non-Goals:
- Dependencies:
- Expected Deliverables:
- Definition of Done:

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, docs/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1:
- [ ] Stage 2:
- [ ] Stage 3:

## Stage Log
- 2026-08-12 06:42:31 Closed the last PR #34 residual: a registry lock stall at run completion could strand run-held resources forever because holder binding is fail-closed and the CLI releases only conversation-held leases. run start's hygiene pass now always releases resource leases whose holding run is terminal or pruned (external lock removed first, registry updated under the lock with idempotent status re-check; failures mark release_failed for manual attention); the supervisor finalize release also retries 3x before deferring to the self-heal. Regression covers terminal-holder, missing-holder, live-holder, and conversation-holder matrices.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Resource leases orphaned by finished runs now self-heal: the run start hygiene pass releases any resource lease whose holding run is terminal or pruned, independent of the retention setting, and the supervisor finalize release retries before deferring to it. Previously a single registry lock stall at run completion stranded the resource forever since holder binding is fail-closed.
- Tests: new orphan-release regression OK; retention and resource-hold regressions OK (3/3 in 5.5s); execution-lease file 19/19 in 64.6s; py_compile OK
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-08-12 06:42:31
- Completed-at-ns: 1786488151979064000
