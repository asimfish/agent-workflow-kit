# T-005 - loop feedback drives next cycle via follow-up packets

Status: done
Owner: codex
Agent: codex
Created: 2026-07-02 17:43:17
Updated: 2026-07-02 17:43:17

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: close the Feedback link of loop engineering — a failed/strict-partial checkpoint automatically creates a follow-up packet in the bus, the next loop run reads the previous run's state, and a later successful checkpoint auto-closes its follow-up.
- Non-Goals: cron/daemon scheduling; automatic code fixes; generic executor for custom loops (that is T-006).
- Dependencies: existing loop runtime (T-002/T-003) and handoff bus.
- Expected Deliverables: updated `tools/agentctl.py` (checkpoint follow-up create/close, previous-state injection into built-in loops and run reports), updated `docs/loop-engineering.md`, template copy of agentctl distributed via existing init path.
- Definition of Done: a strict checkpoint that fails leaves exactly one open follow-up packet (no duplicates on re-run); a subsequent successful checkpoint marks it done; loop run reports contain a `Previous:` line; `agentctl check --mode manual` passes.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/agentctl.py, .agent/, docs/`
- Files likely to touch: `tools/agentctl.py`, `docs/loop-engineering.md`, `.agent/tasks/T-005.md`, `.agent/loops/*` (runtime artifacts)
- Files explicitly out of scope: `templates/` except loop docs sync, experiment directories

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: previous-state injection — loop runs receive the prior run entry; reports gain a `Previous:` line and resolved/persisting feedback.
- [x] Stage 2: checkpoint follow-up packets — failed (or strict partial) checkpoints create a deduplicated bus packet; successful checkpoints auto-close it.
- [x] Stage 3: surface open loop follow-ups in daily-plan-triage; update docs; verify with agentctl check + manual scenario test.

## Stage Log
- 2026-07-02 23:37:22 Stage 3 done: daily-plan-triage surfaces open follow-ups with resolution hint; docs/loop-engineering.md gains Feedback Link section; DoD scenario test passed (fail -> 1 packet, re-fail -> occurrences=2 no duplicate, success -> auto-closed to bus/done; check --mode manual OK).
- 2026-07-02 23:37:22 Stage 1-2 verified: agentctl.py adds previous-state injection (Previous: line + resolved/persisting/regression feedback) and checkpoint follow-up packets (create on failed/strict-partial, dedup via occurrences, auto-close on success; state.json mirrors open_follow_up).

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - DoD scenario: `loop auto --checkpoint pre-finish --once --force` twice with a malformed task doc (expect 1 packet, occurrences=2), then once after fixing (expect auto-close).
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.
- Result 2026-07-02: check --mode manual OK; scenario evidence in `.agent/bus/done/20260702-233445-loop-pre-finish-to-T-005.json` (occurrences=2, auto-closed) and run reports `.agent/loops/runs/20260702-2334*..2336*`; `Previous:` line present in all new run reports.

## Completion Record
- Summary: Closed the loop Feedback link: previous-state injection into loop runs/reports and deduplicated checkpoint follow-up packets with auto-close on success; daily-plan-triage surfaces open follow-ups; docs updated.
- Tests: agentctl check --mode manual (OK); DoD scenario fail->dedup->auto-close via pre-finish checkpoint (evidence in .agent/bus/done/20260702-233445-loop-pre-finish-to-T-005.json)
- Completed-at: 2026-07-02 23:38:22
