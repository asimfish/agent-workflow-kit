# T-006 - generic executor for custom loop contracts

Status: done
Owner: codex
Agent: codex
Created: 2026-07-02 23:40:28
Updated: 2026-07-02 23:40:28

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: let custom (non-built-in) loop contracts declare executable check commands that `agentctl loop run <id> --once` actually runs, deriving the run status from exit codes, so projects can add project-specific loops without editing agentctl.
- Non-Goals: cron/daemon scheduling; parallel loop execution; unbounded or interactive commands; automatic fixes for failing checks.
- Dependencies: loop runtime (T-002/T-003) and feedback link (T-005); previous-state injection and checkpoint follow-up packets must keep working for custom loops.
- Expected Deliverables: agentctl support for a machine-readable command block in loop contracts (with per-command timeout and output capping), updated `docs/loop-engineering.md`, an example custom loop in installed templates.
- Definition of Done: a custom loop with declared check commands runs via `loop run <id> --once`, its report reflects command results and carries a `Previous:` line; a failing command yields `failed`/`partial` and feeds checkpoint follow-up packets; `agentctl check --mode manual` passes.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/agentctl.py, .agent/, docs/, templates/project/.agent/loops/`
- Files likely to touch: `tools/agentctl.py`, `docs/loop-engineering.md`, `.agent/tasks/T-006.md`, `.agent/loops/*` (runtime artifacts), `templates/project/.agent/loops/` (example loop)
- Files explicitly out of scope: experiment directories; hooks

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: define the command block format in loop contracts and parse it (with validation in `agentctl check`).
- [x] Stage 2: execute declared commands in `loop run --once` with timeout + output capping; map exit codes to loop status.
- [x] Stage 3: ship an example custom loop template, update docs, and verify end to end (custom loop failure creates a checkpoint follow-up when run via a checkpoint).

## Stage Log
- 2026-07-02 23:59:34 Stage 3 done: project-check example loop shipped in repo + templates/project; docs/loop-engineering.md adds Custom Loop Commands section; e2e verified success run, failing command -> checkpoint follow-up packet (dedup) -> auto-close after fix, previous-state injection on custom loops, malformed block flagged by run + check; temp scenario artifacts cleaned.
- 2026-07-02 23:59:34 Stage 1-2 done: loop-check fence parser (_loop_command_spec) with timeout/max-output options + validation in agentctl check; executor (_loop_run_commands) runs declared commands with timeout and output capping, exit codes decide status; wired into _run_loop_once for non-builtin loops; builtin ids reject command blocks.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 tools/agentctl.py loop run project-check --once`
  - DoD scenario: temp loop with `$ exit 3` behind a temp checkpoint -> follow-up packet created and deduplicated; switch to `$ true` -> checkpoint success auto-closes packet; malformed block -> run failed + check reports it.
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.
- Result 2026-07-02: all passed. Evidence: `.agent/loops/runs/20260702-2356*..2358*` (project-check success with Previous: line; tmp-t006-fail failed/persist/success sequence), `.agent/bus/done/20260702-235734-loop-custom-check-to-T-006.json` (occurrences=2, auto-closed), check --mode manual OK after cleanup.

## Completion Record
- Summary: Custom loop contracts can declare a loop-check command block that agentctl executes with timeout and output capping; exit codes decide run status, malformed blocks fail the run and are flagged by check, project-check ships as installed example, docs updated.
- Tests: py_compile; check --mode manual (OK); loop run project-check --once (success); DoD scenario fail->dedup->auto-close via temp checkpoint; malformed-block run+check paths
- Completed-at: 2026-07-03 00:00:34
