# T4C7B83C03F3DE7D5-001 - independent review of orphaned resource self-heal

Status: done
Owner: independent-reviewer-2
Agent: independent-reviewer-2
Created: 2026-08-12 06:48:15
Updated: 2026-08-12 06:48:15

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal:
- Non-Goals:
- Dependencies:
- Expected Deliverables:
- Definition of Done:

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1:
- [ ] Stage 2:
- [ ] Stage 3:

## Stage Log
- 2026-08-12 06:48:34 Independent review of 78465ba (TA08B0CC413F151F5-008, orphaned resource self-heal): all four adversarial points verified. (1) Double release is safe: the locked update re-checks status and skips released/release_failed rows, and _external_resource_release local-mkdir catches FileNotFoundError (owner.json unlink + rmdir in one try) so concurrent releases converge to released; supervisor _resource_release_by_id short-circuits already-released. (2) Live runs are protected by the terminal-status filter (only succeeded/failed/cancelled or missing holders release; any other status incl. starting/stopping is skipped), and the retention prune at agentctl.py:6356-6359 only removes terminal run leases, so missing-due-to-prune implies terminal. (3) Overhead is one unlocked registry read per run start with a locked pass only when candidates exist. (4) Finalize retry is bounded (range(3), 1s/2s backoff, then defers to self-heal). Evidence: py_compile clean; 3/3 targeted regression tests OK in 7.9s. Residuals, none blocking: (a) acquire-before-register window: _run_start acquires run-held resources (line 6794) before appending the run lease (6855, same pattern in adopt path 6992/7020), so a concurrent run-start hygiene pass reading in that ms-scale window sees a missing holder and can release a just-acquired lock, breaking mutual exclusion; the locked update re-checks resource status but not holder existence. Suggest a grace period on missing-holder candidates (skip resource leases younger than a few minutes) or registering the run lease before acquiring. (b) ssh-mkdir asymmetry: in the primary stall scenario the external dir is already gone, remote rmdir fails, and the lease lands in release_failed (permanent manual-attention) though the resource is actually free; local-mkdir heals cleanly via FileNotFoundError->None. (c) release-by-path does not verify owner.json identity before rmdir (pre-existing pattern). Verdict: approve; fix is a strict improvement over the permanent-strand status quo.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Independently reviewed commit 78465ba (orphaned resource self-heal, TA08B0CC413F151F5-008) and approved the gate. Verified: double release is idempotent (locked status re-check, FileNotFoundError-safe external rmdir), live and conversation holders are never released, retention prune only removes terminal run leases (missing holder implies pruned-terminal after registration), overhead is one unlocked read per run start, finalize retry bounded at 3 attempts. Non-blocking residuals recorded: acquire-before-register window in run start/adopt paths can expose a just-acquired resource to misclassification under concurrent run starts (suggest missing-holder grace period), and ssh-mkdir stall-heal lands in release_failed instead of released.
- Tests: python3 -m py_compile tools/agentctl.py clean; python3 -m unittest tests.test_execution_lease_workflow (test_resources_orphaned_by_finished_runs_are_released, test_supervised_run_holds_resource_then_releases_it_after_success, test_peer_cannot_force_release_another_conversations_resource): Ran 3 tests in 7.910s OK
- Worker-runtimes: host-runtime:aa9f55979583b6fbda5e433c792f9a30
- Completed-at: 2026-08-12 06:48:59
- Completed-at-ns: 1786488539128160000
