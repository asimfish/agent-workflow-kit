# T3D6917C0AC71CAAE-001 - isolate stale session admission and supervise idle GPU runs

Status: done
Owner: codex
Agent: codex
Created: 2026-08-11 16:52:41
Updated: 2026-08-11 16:52:41

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: prevent identifiable stale peer sessions from globally blocking
  unrelated work selection, and add a lease-first, progress-aware supervision
  path for GPU resources held by durable runs.
- Non-Goals: silently releasing stale session claims; killing a process from one
  zero-utilization sample; embedding a cluster-specific scheduler; modifying or
  stopping any live external experiment during implementation.
- Dependencies: build on the unified session, run, resource, worktree, and loop
  leases introduced by TB0621800D60E51C4-001; preserve holder-bound release and
  fail-closed identity guarantees.
- Expected Deliverables: conflict-scoped stale-session migration reporting;
  configurable resource telemetry and supervision state transitions; focused
  negative/positive regressions; an accepted ADR and operator documentation;
  installed-controller behavior; a replay assessment against the four
  supplied historical conversations.
- Definition of Done: identifiable stale peers are reported without making
  `migrate` fail, while same-task/overlapping-scope work remains rejected by the
  admission guard; a registered GPU resource can move through active, suspected,
  grace, and reclaimable states only after consecutive low-utilization plus
  missing-progress evidence; progressing, phase-exempt, or probe-error resources
  are never marked reclaimable; focused and full suites, installed-copy parity,
  manual checks, and the
  four-history replay matrix pass or record explicit residual gaps.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/tools/, tests/, docs/, README.md`
- Files likely to touch: `tools/agentctl.py`, focused execution-architecture
  tests, `docs/architecture/`, and workflow/operator docs; `init` installs the
  controller directly from the kit copy.
- Files explicitly out of scope: active experiment repositories, GPU processes,
  external schedulers, user credentials, and unrelated GitNexus-generated files.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: recorded ADR-001 and captured three exact red regressions.
- [x] Stage 2: implemented advisory stale migration plus lease-first GPU supervision.
- [x] Stage 3: verified behavior and replayed four histories; ready to finish and publish.

## Stage Log
- 2026-08-11 18:14:02 Final verification: 189/189 tests pass; manual check,
  reconcile, py_compile, diff-check, installed-controller tests, process-tree
  cleanup, and four-history replay pass. GitNexus reports the expected HIGH
  central-controller surface (51 symbols, 15 run/resource flows) with no
  out-of-scope flow.
- 2026-08-11 17:20:04 Pre-publish review repaired three verified boundaries: watchdog children now run in dedicated process groups for descendant-safe termination; heartbeat cadence is independent from configurable telemetry cadence; globally enabled GPU policy skips CPU-only runs and malformed numeric policy fails without traceback. Four-conversation replay is recorded with conditional residuals for unmanaged systemd/SSH jobs, external contamination, missing runtime files, and scheduler placement.
- 2026-08-11 17:08:16 Stages 1-2 complete: ADR accepted; 24 migration/execution tests plus legacy stale-fence regression pass. Entering full verification and four-history replay.
- 2026-08-11 17:25:00 Migration and execution modules pass 24/24; owner binding,
  remote terminate refusal, probe-error fail-safe, phase exemption, progress
  preservation, idle reclaim, and legacy stale-conflict fencing are green.
- 2026-08-11 16:59:29 Stage 1 red evidence: stale migrate returns inspect_stale rc1; watchdog transition is absent; run start rejects GPU policy flags. Three focused regressions fail for those exact causes.
- 2026-08-11 16:53:09 Frozen acceptance and replay gate: stale migration advisory with fail-closed admission; GPU reclaim requires registered ownership, consecutive low utilization, and missing progress.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - focused session migration and resource supervision tests
  - `python3 -m unittest discover -s tests -p 'test_*.py'`
  - template parity and `python3 -m py_compile` checks
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Scoped identifiable stale peers to migration warnings while preserving conflict admission; added opt-in progress-aware GPU lease supervision with durable heartbeats, safe process-tree reclamation, operator visibility, ADR, and four-history replay.
- Tests: 189/189 unittest discovery; 26 focused migration/execution/identity tests; manual check OK; reconcile OK; py_compile and diff-check OK; GitNexus final detect-changes reviewed (expected HIGH: 51 symbols, 15 run/resource flows).
- Worker-runtimes: host-runtime:db714698c615186e84fdfd5d78faf905
- Completed-at: 2026-08-11 18:14:55
- Completed-at-ns: 1786443295984014000
