# T-092 - skip contamination scan when no peers to avoid over-block and cost

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-20 12:40:16
Updated: 2026-07-20 12:40:16

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Remove an over-security/efficiency cost: the workspace contamination scan ran on every mutating guard call even for a session alone in its checkout, both blocking legitimate out-of-scope tracked edits (e.g. a build touching shared.cfg) and paying a git status on every write. Skip the scan when no peer session is live.
- Non-Goals: Weakening protection when peers ARE present; changing path/opaque/git classification.
- Dependencies: T-081 contamination scan; T-088 scope containment.
- Expected Deliverables: Peer-gated contamination scan, a solo-not-blocked regression, an updated multi-session contamination test that keeps a peer live, docs note.
- Definition of Done: Solo sessions are never contamination-blocked and skip the git status; multi-session contamination still blocks; suites pass; independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Failing regression that a solo session is not contamination-blocked.
- [x] Stage 2: Gate the scan on live peers; update the multi-session contamination test to keep a peer live; docs.
- [x] Stage 3: Focused/full validation and independent gate.

## Stage Log
- 2026-07-20 12:51:38 Contamination scan now only runs when a peer session is live: fixes solo over-block and skips git status on the solo write path; multi-session protection unchanged.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Contamination scan is gated on live peers: solo sessions no longer over-block on out-of-scope tracked edits and skip the git status on the write path; multi-session protection unchanged.
- Tests: solo/peer regression red->green; focused 62/62; full discover 135/135; py_compile OK; git diff --check OK; agentctl check manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-20 13:00:58
- Completed-at-ns: 1784523658283792000
