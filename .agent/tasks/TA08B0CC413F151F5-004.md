# TA08B0CC413F151F5-004 - persist supervised run terminal state through lock contention

Status: review
Owner: cursor
Agent: cursor
Created: 2026-08-12 03:25:58
Updated: 2026-08-12 03:25:58

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal:
- Non-Goals:
- Dependencies:
- Expected Deliverables:
- Definition of Done:

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1:
- [ ] Stage 2:
- [ ] Stage 3:

## Stage Log
- 2026-08-12 04:12:14 Root causes closed: (1) supervisor claim and terminal-state writes crashed on a single 10s registry lock timeout, leaving leases permanently non-terminal (the CI exited_unknown flake); now claim retries 4x with backoff, terminal/failure writes retry 6x, and periodic heartbeats/telemetry tolerate a missed beat. (2) A supervisor dying before claiming was silent and unrecoverable; run start now confirms the claim, respawns one replacement supervisor on pre-claim death, fails the lease with the supervisor stderr log tail if both die, and supervisor stderr persists to <lease>.supervisor.log. Deterministic regressions: 13s flock hold across child exit (red on old controller: both contention tests burned their 60s waits), watcher unit coverage for died/pending/claimed, claim-at confirmed in run show after start.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Supervised runs now survive registry lock contention and pre-claim supervisor deaths: claim and terminal writes retry with backoff, heartbeats tolerate missed beats, run start confirms the supervisor claim and respawns once before failing closed with a persisted supervisor stderr log.
- Tests: full suite 192/192 in 696.4s; execution-lease file 3x consecutive 16/16 (63-69s); red-proof: both contention regressions fail on the pre-fix controller (stash test, 2 failures burning 60s waits); py_compile OK
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-08-12 04:12:14
- Completed-at-ns: 1786479134714847000
