# T-060 - independent gate review for T-057 and T-059

Status: done
Owner: independent-reviewer
Agent: independent-reviewer
Created: 2026-07-19 01:52:33
Updated: 2026-08-27 15:53:05

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Independently gate the complete T-057/T-059 candidate against `origin/main`, with emphasis on the previously reproduced terminal-only controller and hook bypass and the full session/migration isolation contract.
- Non-Goals: Editing implementation, tests, README, docs, templates, Git history, branches, remotes, or any path outside `.agent/`; fixing findings; releasing another session; committing or pushing.
- Dependencies: T-057, the T-058 P1 rejection evidence, and the completed T-059 remediation must remain available as one candidate diff in this checkout.
- Expected Deliverables: Findings-first code and policy audit; exact direct-controller and PreToolUse fresh-install reproductions; parser-leaf default-deny coverage review; focused and environment-scrubbed full tests; static/manual checks; GitNexus change-risk evidence; independent workflow gate decisions for T-057 and T-059.
- Definition of Done: Any actionable issue is recorded and causes a specific gate rejection; otherwise concise independent evidence is added to T-057 and T-059, their final Stage 3 evidence is completed, both gates approve as `independent-reviewer`, and T-060 is finished without any non-`.agent/` write.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/`
- Files likely to touch: `.agent/tasks/T-060.md`, `.agent/tasks/T-057.md`, `.agent/tasks/T-059.md`, gate records, and workflow-managed `.agent/` indexes/logs.
- Files explicitly out of scope: `tools/`, `tests/`, `templates/`, `README.md`, `docs/`, implementation fixes, Git history, branches, remotes, and all other non-`.agent/` paths.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Inspect the complete `origin/main` diff, command-policy leaves, trust boundaries, documentation parity, and GitNexus risk evidence.
- [x] Stage 2: Run exact fresh-install fail-closed probes, focused/full tests with identity environment scrubbed, and all required static/manual checks.
- [x] Stage 3: Record findings and issue independent workflow gate decisions for T-057 and T-059, then finish T-060.

## Stage Log
- 2026-07-19 02:09:20 Independently rejected T-059 and T-057 from T-060 with runtime-bound gate records; both remain blocked pending the two verified P2 policy fixes and regressions.
- 2026-07-19 02:07:39 Canonical terminal-only task create now blocks in hook and controller with no task write, but adversarial review found two P2 contract gaps: python -m tools.agentctl is default-allowed by PreToolUse, and terminal-only sessions list creates the generated session view despite init being claimed as the sole bootstrap mutation.
- 2026-07-19 01:53:13 Established distinct independent-reviewer session T-060, registered its review role, and recorded the .agent-only gate contract and verification matrix.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - exact fresh-install direct-controller and PreToolUse `task create` reproductions
  - `python3 -m unittest -v tests.test_multi_session_workflow tests.test_migrate_workflow tests.test_install_and_hooks`
  - `env -u <all provider/session identity variables> python3 -m unittest discover -s tests -p 'test_*.py' -v`
  - `python3 -m py_compile <changed Python files>` with bytecode redirected outside the worktree
  - `git diff --check origin/main --`
  - `python3 tools/agentctl.py check --mode manual`
  - GitNexus changed-symbol and execution-flow audit or inspection of current matching audit evidence
- Expected result:
  - Terminal/default identity fails closed for every mutation except `init`; trusted sessions work; session/fork/clone/migration behavior remains isolated and non-destructive; root/template copies and upgrade guidance agree; all required checks pass.

### Findings

- P2 verified: PreToolUse is not default-deny for every valid AgentCTL shell invocation. With only `TERM_SESSION_ID`, the canonical `python3 tools/agentctl.py task create ...` is blocked, but `python3 -m tools.agentctl task create ...` returns no hook decision. The controller still exits 2 and writes no T-902 task, so the second layer prevents this reproduction from mutating state; however, the matching hook policy and parser-leaf default-deny contract are not met. Recognize module execution or fail closed whenever an AgentCTL-looking command cannot be classified, and add a regression.
- P2 verified: `init` is not literally the sole identity-free file mutation. In a fresh install with only `TERM_SESSION_ID`, `.agent/state/SESSIONS.md` was absent before `agentctl sessions list`; that identity-free command exited 0 and created a 510-byte generated view through `_render_sessions_view`. Either keep untrusted `sessions list` read-only, require trusted identity for its render, or narrow the documented/acceptance claim explicitly.

### Results

- Complete candidate inspection covered 21 tracked changed files plus all untracked task/gate/loop records and `tests/test_migrate_workflow.py`. GitNexus `detect-changes --scope compare --base-ref origin/main` reported 104 changed symbols, 47 affected flows, and CRITICAL risk. `_load_session` has 37 upstream dependents (19 direct, 10 processes); `_workflow_session_key` has 49 (12 direct, 10 processes).
- Exact fresh-install canonical replay under `env -i ... TERM_SESSION_ID=shared-terminal-window`: `init` exited 0; direct `task create T-901` exited 2; PreToolUse returned `decision=block`; the task file and all plan/index/board references remained absent.
- Independent argparse traversal found 49 leaves: 31 identity-required, 18 allowlisted, zero controller/hook mismatches for canonical file-path invocation, and zero unmapped action subparsers.
- Focused scrubbed tests passed 37/37; full scrubbed discovery passed 110/110. Coverage includes trusted sessions, same-terminal separation, fork/same-ID clone isolation, full Git-clone locality, stale claim preservation, legacy migration, install preservation, and the canonical T-058 reproduction.
- `py_compile` passed for all 11 changed Python files with `PYTHONPYCACHEPREFIX` outside the worktree. `git diff --check` and `git diff --check origin/main --` exited 0. `agentctl check --mode manual` reported OK; `doctor --json` reported OK; current `migrate --json` returned `continue` with no changed documents.
- Root/template hook copies are byte-identical at SHA-256 `5bd2b841a0a71317733e97296bfb0bd4935a7dc1d39e2954c68c7a0cc0331233`; workflow-entry copies are byte-identical at `efc48bf19013367beedf9d3ab44ddc9f55d1169c827bc0338633a8edbc0bfc4e`. Upgrade instructions match the implemented actions, and the install regression preserved project-owned plan history.

## Completion Record
- Summary: Independent gate review completed; exact canonical T-058 P1 is fixed, but verified module-form hook default-allow and terminal-only sessions-list write require T-057/T-059 to remain blocked.
- Tests: fresh-install direct/hook canonical replay passed with no T-901; argparse matrix 49 leaves; focused scrubbed unittest 37/37; full scrubbed unittest 110/110; all 11 changed Python files py_compile; git diff --check and base diff check; manual check; doctor; migrate=continue; root/template cmp; GitNexus 104 symbols/47 flows CRITICAL; adversarial P2 probes reproduced
- Worker-runtimes: host-runtime:4ccaac37cc0ef081d59df6ac76906393
- Completed-at: 2026-07-19 02:09:57
- Completed-at-ns: 1784398197002668000

## Administrative Closure (2026-08-27 15:53:05)

- Closed by supervisor during board hygiene (TR review task, 2026-08-27): the underlying work was merged long ago; these entries predate the independent-gate record format, so no gate file exists to auto-close them.
