# T-082 - independently review T-081 concurrent isolation hardening

Status: review
Owner: independent-reviewer
Agent: independent-reviewer
Created: 2026-07-20 02:35:55
Updated: 2026-07-20 02:35:55

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Independently review T-081 and draft PR #17 from this distinct `independent-reviewer` runtime, with special attention to cross-scope write escapes, Git/GitHub mutation classification, source/template parity, and executable concurrent-worktree guidance.
- Non-Goals: Changing implementation or tests to obtain a pass, merging the pull request, writing outside `.agent/`, or claiming a reviewer identity/runtime that did not perform this review.
- Dependencies: T-081 task evidence, commit `8ac5e64`, the complete PR #17 diff, README/docs, source and installed Hook/controller templates, and relevant regression tests.
- Expected Deliverables: A code-and-threat-model review, independent focused/full/adversarial verification, an evidence-backed T-081 gate decision, a finished T-082 review record, and one pushed workflow-metadata commit referencing T-082.
- Definition of Done: Any blocking finding is recorded precisely and causes `gate reject`; otherwise all required evidence passes, T-081 is approved by this active independent runtime, T-082 records the conclusion and is finished, and only `.agent/` workflow metadata is committed and pushed without merging PR #17.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/`
- Files likely to touch: `.agent/tasks/T-082.md`, `.agent/gates/T-081.md`, and controller-generated task/board/log/loop metadata.
- Files explicitly out of scope: `tools/`, `templates/`, `tests/`, `README.md`, `docs/`, application code, and all PR implementation changes.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Inspected T-081, commit `8ac5e64`, the complete PR #17 diff, README/docs, Hooks/controllers, templates, and tests; blocking classification and migration findings remain.
- [x] Stage 2: Ran the new focused regression and fresh-install classification probes plus static hygiene; stopped the approval-only full suite and py_compile after blockers under the explicit administrative-only instruction.
- [x] Stage 3: Recorded findings and evidence and rejected T-081; T-082 is ready for finish and review-metadata handoff.

## Stage Log
- 2026-07-20 02:53:37 Gate rejected T-081 to blocked as independent-reviewer; follow-up requires closing every demonstrated cross-scope/embedded-execution gap, adding dual-session regressions, reconciling WORKFLOW_ENTRY guidance, and rerunning the full matrix.
- 2026-07-20 02:53:03 Independent review found blocking cross-scope gaps: xxd stdin-output, mv source and multi-file in-place targets, curl/Git/GH embedded writes or execution, and dynamic/prefix path attribution are still admitted; current WORKFLOW_ENTRY also retains non-executable active-task worktree guidance. Focused new regression and static hygiene passed, but approval is rejected pending fixes and regressions.
- 2026-07-20 02:40:16 Established T-082 independent review contract: inspect T-081/PR #17 threat boundaries, run focused/full/adversarial evidence without implementation edits, and gate reject on any blocking finding or approve only after all checks pass.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 -m unittest tests.test_multi_session_workflow tests.test_worktree_workflow tests.test_install_and_hooks`
  - `python3 -m unittest discover -s tests`
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py templates/project/tools/agent_workflow_hook.py`
  - `cmp tools/agent_workflow_hook.py templates/project/tools/agent_workflow_hook.py`
  - `cmp .agent/WORKFLOW_ENTRY.md templates/project/.agent/WORKFLOW_ENTRY.md`
  - `git diff --check`
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Known option-based and embedded-execution write escapes are denied, Git/GitHub mutations are not read-only, concurrent shared-checkout guidance names commands that can actually run, source/template behavior is migration-safe, and every required check passes before approval.

### Independent Results

- 🟢 `verified`: PR #17 is draft, contains only commit `8ac5e642ca2b3a95ca1e87b274e953c6b4960be1`, and its remote head matches the reviewed local HEAD; the complete 16-file diff was inspected.
- 🟢 `verified`: `test_read_only_commands_fail_closed_on_output_and_execution_options` passed (`Ran 1 test`, `OK`), so the new listed regression cases are green.
- 🟢 `verified` blocking gap: fresh-install dual-session Hook checks still allowed `xxd -r - src/two/dump.bin` as `read_only` with no path; a disposable execution created the peer-scope output file. `mv src/two/a src/one/a` and multi-file `sed`/`perl` in-place forms likewise checked only the own-scope destination/final operand while missing peer-scope source deletion or earlier modified files.
- 🟢 `verified` blocking gap: `curl --write-out '%output{src/two/leak.txt}' ...` and `git -c diff.external='python3 evil.py' diff` were classified `read_only` and allowed beside a peer; disposable executions created the requested curl output and ran the external-diff helper. `git stash show --ext-diff` and `gh pr view 1 --web` were also admitted despite embedded-execution surfaces.
- 🟢 `verified` blocking gap: prefix/dynamic path forms such as `env --chdir=src/two touch src/one/ok`, a bare `ln -s src/one/a`, variable-expanded targets, and brace-expanded targets were admitted with incomplete or incorrect path attribution.
- 🟢 `verified` migration/documentation gap: `.agent/WORKFLOW_ENTRY.md` differs from `templates/project/.agent/WORKFLOW_ENTRY.md`; the current repository entry still tells an already-active shared-checkout task to run from a task worktree, while `worktree create` requires a clean committed `todo`/`ready` task and the corrected template states that precondition.
- 🟢 `verified`: Hook source/template `cmp`, `git diff --check`, and `python3 tools/agentctl.py check --mode manual` passed.
- Not run after the blocking decision and explicit stop instruction: the broader focused/full suites and py_compile matrix. No pass is claimed for them in T-082.

## Completion Record
- Summary: Independently reviewed T-081/PR #17 and rejected the gate after verified cross-scope write, embedded-execution, path-attribution, and WORKFLOW_ENTRY migration blockers.
- Tests: PASS: focused read-like regression (1 test), Hook source/template cmp, git diff --check, agentctl check --mode manual. FAIL/blocker: current/template WORKFLOW_ENTRY cmp. NOT RUN after explicit stop: broader suites and py_compile.
- Worker-runtimes: host-runtime:8b24638007e8b86be40fdbfad4ad0cb6
- Completed-at: 2026-07-20 02:53:55
- Completed-at-ns: 1784487235853087000
