# T-003 - add continuous loop automation

Status: review
Owner: codex
Agent: codex
Created: 2026-07-01 18:39:14
Updated: 2026-07-01 18:39:14

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Make the loop system continuous at workflow checkpoints so agents automatically run the relevant bounded loop when starting work, finishing work, or monitoring experiments.
- Non-Goals: Do not add a daemon, cron scheduler, parallel worktree pool, external connector, or automatic expensive experiment launch in this phase.
- Dependencies: Existing `agentctl work`, `agentctl finish`, lifecycle hook bridge, `.agent/loops/` contracts, and Git hook checks.
- Expected Deliverables: `agentctl loop auto` or equivalent checkpoint runner, startup/finish/experiment loop policy in `.agent`, docs/templates updated, and install smoke tests proving loops run in a fresh target project.
- Definition of Done: A fresh installed project can run checkpoint loops without human commands; `work` can trigger plan triage, `finish` can trigger document hygiene, explicit experiment checkpoints can trigger experiment monitoring, every run writes loop memory, and workflow checks pass.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/agentctl.py, tools/agent_workflow_hook.py, .agent/, templates/project/.agent/, README.md, docs/`
- Files likely to touch: `tools/agentctl.py`, `tools/agent_workflow_hook.py`, `.agent/loops/`, `templates/project/.agent/loops/`, `README.md`, `docs/loop-engineering.md`, `docs/workflow.md`
- Files explicitly out of scope: SafeTransport/SafeOT project files, background services, external hosted schedulers, and automatic experiment execution.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Design and implement checkpoint loop policy.
- [x] Stage 2: Wire startup/finish/experiment checkpoint runners.
- [x] Stage 3: Update docs/templates and verify fresh install behavior.

## Stage Log
- 2026-07-01 18:54:42 Verified enhanced daily-plan-triage passes after adding missing PROJECT_PLAN.md rows for active review tasks.
- 2026-07-01 18:54:14 Reopened T-003 to fix a plan-triage gap: PROJECT_PLAN.md was missing T-001/T-003 task board rows, and daily-plan-triage now reports board tasks missing from the plan.
- 2026-07-01 18:50:39 Fresh install smoke test passed: work auto-ran work-start, finish auto-ran pre/post doc hygiene, experiment checkpoint ran explicitly, and same-second doc-hygiene reports used unique filenames.
- 2026-07-01 18:48:20 Validated checkpoint runs in this repo; added loop-state locking after concurrent checkpoint runs exposed a real state overwrite risk.
- 2026-07-01 18:45:36 Implemented checkpoint loop policy and wired the intended workflow nodes: work-start, pre-finish, post-finish, and explicit experiment-check.
- 2026-07-01 18:39:47 Defined T-003 scope: implement checkpoint-triggered continuous loops without adding daemon, cron, worktree pool, or automatic expensive experiment launches.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py`
  - `python3 tools/agentctl.py loop auto --checkpoint work-start --once`
  - `python3 tools/agentctl.py loop auto --checkpoint pre-finish --once`
  - temporary install smoke test for checkpoint loop behavior
- Commands run:
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py`
  - `python3 tools/agentctl.py loop auto --checkpoint work-start --once --force`
  - `python3 tools/agentctl.py loop auto --checkpoint pre-finish --once --force`
  - `python3 tools/agentctl.py loop auto --checkpoint experiment-check --once --force`
  - `python3 tools/agentctl.py work --agent codex`
  - `python3 tools/agentctl.py loop auto --checkpoint work-start --once --force` after adding missing-plan-row triage
  - temporary install smoke test proving `work` triggers `work-start`, `finish` triggers `pre-finish` and `post-finish`, `experiment-check` runs explicitly, and same-second reports do not overwrite each other
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Added checkpoint-triggered continuous loops with project-local policy, automatic work/finish loop hooks, loop-state locking, unique run reports, plan-row triage checks, docs/templates, and install smoke coverage.
- Tests: python3 tools/agentctl.py check --mode manual; python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py; git diff --check; python3 tools/agentctl.py loop auto --checkpoint work-start --once --force; python3 tools/agentctl.py loop auto --checkpoint pre-finish --once --force; python3 tools/agentctl.py loop auto --checkpoint experiment-check --once --force; python3 tools/agentctl.py work --agent codex; fresh install checkpoint smoke tests
- Completed-at: 2026-07-01 18:55:07
