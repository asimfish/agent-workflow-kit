# T-043 - fix Windows guidance receipt integrity CI

Status: done
Owner: codex
Agent: codex
Created: 2026-07-18 02:16:07
Updated: 2026-07-18 02:16:07

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Restore deterministic guidance receipt integrity on Windows and remove the same binary-key risk from eval signing.
- Non-Goals: Change dispatch contracts, signature formats, acceptance policy, or multi-session behavior.
- Dependencies: PR #7 contains the independently reviewed multi-session implementation; its latest Windows job exposed the failure.
- Expected Deliverables: Binary-safe exclusive secret creation and a deterministic regression covering exact random bytes for guidance and eval keys.
- Definition of Done: Local guidance/full regressions and workflow checks pass, PR #7 Windows and Linux jobs pass on the new head, and the PR is merged to main.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, templates/project/, .agent/`
- Files likely to touch: `tools/agentctl.py`, `tests/test_guidance_workflow.py`, and T-043 workflow records.
- Files explicitly out of scope: Dispatch semantics, session coordination policy, and unrelated root-worktree changes.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Reproduce the CI symptom from logs and identify Windows text-mode key creation as the nondeterministic source.
- [x] Stage 2: Implement binary-safe exclusive key creation and run focused/full regressions.
- [ ] Stage 3: Publish the fix, verify all latest PR checks, merge, and verify remote main.

## Stage Log
- 2026-07-18 02:27:01 Fixed Windows binary signing-key persistence for guidance and eval; deterministic exact-byte regression and all 20 guidance tests pass.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-07-18 02:24:00 Confirmed both failing receipts were signed with freshly generated Windows key files and added a binary-mode fix plus deterministic guidance/eval regression.
- 2026-07-18 02:31:00 All 20 guidance tests and all 90 repository tests passed; py_compile, diff check, and manual workflow check also passed.

## Verification

- Commands to run:
  - `python3 -m unittest tests.test_guidance_workflow -v`
  - `python3 -m unittest discover -s tests -v`
  - `python3 -m py_compile tools/agentctl.py tests/test_guidance_workflow.py`
  - `git diff --check`
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Receipt keys preserve exact bytes; all regressions and workflow checks pass on macOS/Linux and the Windows CI runner.

## Completion Record
- Summary: Made guidance and eval signing-key creation binary-safe on Windows and added deterministic exact-byte regression coverage.
- Tests: 20 guidance tests; full 90-test suite; py_compile; git diff --check; agentctl manual check
- Worker-runtimes: host-runtime:8188dc0a82f9eda0bd147ed3c18c35eb
- Completed-at: 2026-07-18 02:31:07
- Completed-at-ns: 1784313067335848000
