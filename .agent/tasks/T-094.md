# T-094 - halve read-only hook latency by removing redundant status spawn

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-20 13:08:03
Updated: 2026-07-20 13:08:03

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Cut read-only PreToolUse latency. Every read command spawned agentctl twice (status --json to decide has_session, then sessions heartbeat). Collapse to one spawn by calling heartbeat directly (it no-ops when there is no active/owned session), preserving liveness and never-block semantics.
- Non-Goals: Changing mutating-path checks; removing the heartbeat (peers need liveness).
- Dependencies: hook pre_tool_use flow.
- Expected Deliverables: single-spawn read-only path, a regression asserting heartbeat still advances and reads never block (with or without a session), docs note optional.
- Definition of Done: Read-only path drops one agentctl spawn (~100ms), heartbeat still advances, idle read still passes, suites pass, independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Regression: read-only hook advances heartbeat and never blocks (session or idle).
- [x] Stage 2: Call heartbeat directly on the read path; drop the has_session status spawn.
- [x] Stage 3: Focused/full validation and independent gate.

## Stage Log
- 2026-07-20 13:15:56 Read-only PreToolUse path now heartbeats in a single agentctl spawn (was two): ~100ms/read saved, liveness and never-block semantics preserved.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Read-only PreToolUse path heartbeats in a single agentctl spawn instead of two (status+heartbeat), cutting ~100ms per read command; liveness and never-block preserved.
- Tests: read-only heartbeat regression; focused 49/49; full discover 136/136; py_compile OK; git diff --check OK; agentctl check manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-20 13:23:45
- Completed-at-ns: 1784525025845217000
