# T-029 - harden installation review gates and native hooks

Status: review
Owner: codex
Agent: codex
Created: 2026-07-15 10:29:18
Updated: 2026-07-15 10:29:18

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Close the identified production-readiness gaps in project adoption,
  independent review gates, native lifecycle hooks, and shipped documentation.
- Non-Goals: Real Fable-to-Codex model execution, an always-on scheduler, and
  protection against an administrator deliberately disabling local hooks.
- Dependencies: Existing init/adoption, task lifecycle, hook bridge, Git hook,
  CI, and unittest infrastructure through T-028.
- Expected Deliverables: An idempotent installer with explicit conflict and
  upgrade behavior; reviewer-bound gate evidence; executable Codex, Claude, and
  Cursor hook contract tests; diagnostics and documentation that state the
  native-hook trust boundary; synchronized root/template agent instructions.
- Definition of Done: Fresh install and repeated upgrade tests pass without
  mixed-version files or silent config loss; self/same-session approval is
  rejected and an independently active reviewer can approve; provider fixtures
  prove allow/block payloads; doctor reports hook wiring limitations; all local
  and CI checks pass.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, templates/project/, integrations/, docs/, README.md, AGENTS.md, .agent/, .codex/, .claude/, .cursor/, .github/, .githooks/, hooks/`
- Files likely to touch: `tools/agentctl.py`,
  `tools/agent_workflow_hook.py`, `tests/`, provider hook configs,
  `README.md`, `docs/enforcement.md`, and matching templates.
- Files explicitly out of scope: Fable/Codex transport behavior and unrelated
  loop execution or experiment code.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Specify and implement safe fresh-install, repeat-install, and
  upgrade/conflict behavior with regression coverage.
- [x] Stage 2: Bind gate decisions to an independent reviewer session and add
  rejection/approval tests.
- [x] Stage 3: Test native provider hook contracts, expose trust/wiring status,
  and synchronize shipped configuration and documentation.
- [x] Stage 4: Run full cross-platform-compatible verification and record
  remaining platform trust boundaries accurately.

## Stage Log
- 2026-07-15 10:36:15 Implemented atomic managed installation, independent reviewer-bound gates, provider hook contract regressions, and synchronized entry/enforcement documentation; 4 focused tests pass.
- 2026-07-15 11:00:00 Full 76-test suite, py_compile, doctor, manual workflow
  check, template parity, and git diff whitespace checks passed.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 -m unittest discover -s tests`
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py`
  - `git diff --check`
- Expected result:
  - Workflow checks and all regressions pass; installation and gate negative
    cases fail closed with actionable messages.

## Completion Record
- Summary: Hardened project installation and upgrades, bound task gates to independent reviewer sessions, added native hook contract diagnostics/tests, and synchronized shipped rules and documentation.
- Tests: 76 unittest regressions; py_compile; agentctl doctor; agentctl check --mode manual; template parity; git diff --check
- Completed-at: 2026-07-15 10:42:11
- Completed-at-ns: 1784083331022697000
