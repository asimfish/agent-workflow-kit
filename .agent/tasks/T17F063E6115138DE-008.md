# T17F063E6115138DE-008 - integrate final main advances for concurrent release

Status: in_progress
Owner: codex
Agent: codex
Created: 2026-07-20 16:04:13
Updated: 2026-07-20 16:04:13

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Integrate `origin/main@741097d` after PRs #26 and #27 so the concurrent
  isolation release preserves eval target-session isolation and warns before
  replacing an existing project's Git hooks.
- Non-Goals: Do not redesign the approved concurrent guards, rewrite history,
  bypass hooks, or alter unrelated task records.
- Dependencies: Merge commit `be61232`; approved integration gate
  `T17F063E6115138DE-006`; main commits `f49f808` and `3c9720c`.
- Expected Deliverables: Conflict-free semantic union, focused regressions for
  any touched behavior, full release checks, ordinary branch pushes, and a
  mergeable PR #25.
- Definition of Done: Both main capabilities and the concurrent-session
  safeguards are present; tests and hooks pass; independent review approves
  any manual semantic conflict resolution; and PR #25 can merge into `main`.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/, tools/, templates/project/, tests/, docs/, README.md, .githooks/, .claude/, .codex/, .cursor/, hooks/, integrations/`
- Files likely to touch: merge-conflicted paths from `tools/`, `tests/`,
  `templates/project/`, and controller-managed `.agent/` release records.
- Files explicitly out of scope: files unrelated to the two new main commits
  or the already approved concurrent release.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1: Merge `origin/main@741097d` and inventory semantic conflicts.
- [ ] Stage 2: Resolve conflicts with impact analysis and verify the union.
- [ ] Stage 3: Obtain independent approval, publish, and merge PR #25.

## Stage Log
- 2026-07-20 16:04:51 Initial merge attempt stopped safely because the new task records were uncommitted; committing the bounded T-008 contract before retrying origin/main integration.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record

- Summary:
- Tests: not run
- Artifacts: none
- Follow-ups: none
