# T-056 - independently verify controller fork fail closed

Status: done
Owner: supervisor
Agent: supervisor
Created: 2026-07-18 11:04:40
Updated: 2026-07-18 11:04:40

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Independently verify that the current controller and hook changes fail
  closed when a fork loses its trusted instance while retaining inherited
  parent workflow identity, then decide the T-051, T-053, and T-055 gates.
- Non-Goals: Modify implementation, templates, tests, documentation, README,
  Git history, branches, pull requests, GitHub, or any file outside `.agent/`.
- Dependencies: T-051 through T-055, including both T-054 gate rejections and
  the uncommitted T-055 implementation/test remediation.
- Expected Deliverables: Current diff review; fresh-install replay for inherited
  explicit-ID and forced-parent-key variants; regression checks for legacy
  ownerless forks, normal resume, sibling isolation, parity, and deterministic
  test environments; the requested full command matrix; concise evidence and
  independent supervisor gate decisions.
- Definition of Done: Read-only hooks remain allowed; child status cannot expose
  a parent task; hook and direct work/start fail closed; the parent record is
  unchanged in both identity variants; all requested regression and parity
  checks pass or affected tasks are rejected with an actionable reproduction;
  T-056 is finished through the workflow.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/`
- Files likely to touch: `.agent/tasks/T-056.md`, `.agent/gates/T-051.md`,
  `.agent/gates/T-053.md`, `.agent/gates/T-055.md`, and workflow-managed
  `.agent/` indexes, logs, state, and loop reports.
- Files explicitly out of scope: implementation, templates, tests, docs,
  `README.md`, Git history, branches, pull requests, GitHub, and all other
  non-`.agent/` paths.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Read T-051 through T-055 and both T-054 gate rejections; inspect
  the current uncommitted implementation/test diff and regression determinism.
- [x] Stage 2: Replay both incomplete-fork identity variants in fresh temporary
  installs and run the complete requested verification matrix.
- [x] Stage 3: Record concise evidence, decide the T-051/T-053/T-055 gates as
  supervisor, and finish T-056.

## Stage Log
- 2026-07-18 11:20:32 Stage 3 gate decisions: independently approved T-051, T-053, and T-055 as supervisor from T-056; all three gate records bind this reviewer task/runtime and a distinct worker runtime. No implementation, template, test, documentation, README, Git, branch, PR, or GitHub state was edited by the reviewer.
- 2026-07-18 11:15:34 Additional fresh-install regressions passed: normal work resumed T-910 on the same key; same-ID siblings received distinct instances and session keys with lineage; legacy ownerless child began empty and owned T-931 while parent stayed on T-930. Focused suites passed 13/13 and 9/9, and the combined 22/22 also passed under deliberately polluted provider/workflow/lineage/isolation environment variables.
- 2026-07-18 11:11:11 Fresh-install replay passed for both missing-instance variants. With inherited explicit ID and, separately, forced parent session key, lineage and an original instance were confirmed; read-only/status hooks remained allowed; direct status returned {}; hook work/start blocked; direct work/start exited 2; Write blocked; parent T-901 status and its sole session-record SHA-256 remained byte-identical.
- 2026-07-18 11:09:10 Stage 1 reviewed T-051 through T-055, both T-054 rejection records, and all five uncommitted code/test files. The controller now returns empty status and rejects work/start on lineage plus a missing instance with inherited explicit or valid forced keys; the hook blocks startup commands but still permits read-only actions. Tests also scrub inherited identity variables; no static blocker found, so both variants require independent replay.
- 2026-07-18 11:05:50 Defined T-056 as an independent .agent/-only review of T-051 through T-055, both T-054 rejections, the exact two fresh-install identity variants, regression coverage, and the requested command matrix.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 -m unittest tests.test_multi_session_workflow`
  - `python3 -m unittest tests.test_install_and_hooks`
  - `python3 -m unittest discover -s tests -p 'test_*.py'`
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py templates/project/tools/agent_workflow_hook.py tests/test_multi_session_workflow.py tests/test_install_and_hooks.py`
  - `cmp tools/agent_workflow_hook.py templates/project/tools/agent_workflow_hook.py`
  - `git diff --check`
  - Fresh temporary-install replay for inherited explicit workflow ID and
    forced parent session key, plus review of legacy ownerless, resume, sibling,
    parity, and deterministic-environment coverage.
- Expected result:
  - All commands pass and neither incomplete-fork variant can observe or resume
    the parent controller session while read-only hook behavior remains usable.
- Results:
  - Independent fresh-install replay passed for inherited explicit-ID and
    forced-parent-key variants. Each child first received lineage and an
    instance; after instance removal, read-only and status hooks were allowed,
    direct status returned `{}`, hook/direct work and start were blocked, and
    parent T-901 plus its sole session record remained byte-identical
    (`f02917eeddf987e134cd27488a044a9b76a68d80e25fc9c9215e0d00012b1431`).
  - Separate fresh-install replay passed for normal resume on the same session
    key, two same-ID siblings with distinct instances/keys, and a legacy
    ownerless fork whose child started empty while the parent stayed unchanged.
  - `tests.test_multi_session_workflow`: 13/13 passed in 29.120 seconds;
    `tests.test_install_and_hooks`: 9/9 passed in 16.951 seconds; full discovery:
    95/95 passed in 145.081 seconds.
  - The two focused modules also passed 22/22 in 47.673 seconds under deliberately
    polluted provider, workflow, instance, lineage, forced-key, isolation-error,
    and terminal identity variables.
  - Manual check, five-file `py_compile`, source/template `cmp`, and
    `git diff --check` all exited 0. No blocking issue remains in the reviewed
    uncommitted controller, source/template hook, or regression-test diff.

## Completion Record
- Summary: Independently verified controller and hook fail-closed behavior, approved T-051/T-053/T-055, and recorded the fresh-install evidence without changing any out-of-scope path.
- Tests: fresh-install explicit-ID and forced-parent-key replays passed with parent record unchanged; normal resume, same-ID sibling, and legacy ownerless fork replays passed; tests.test_multi_session_workflow 13/13; tests.test_install_and_hooks 9/9; polluted-environment focused run 22/22; full discovery 95/95; manual check, five-file py_compile, source/template cmp, and git diff --check passed
- Worker-runtimes: host-runtime:7ac759f84204e3290db893cb7a4b3dac
- Completed-at: 2026-07-18 11:21:38
- Completed-at-ns: 1784344898447800000
