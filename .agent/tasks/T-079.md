# T-079 - verify read-only allowlist strictly and fix git read misclassifications

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-20 00:37:31
Updated: 2026-07-20 00:37:31

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Close the fifth-audit P1s: the read-only allowlist must be verified per command (sort -o and uniq output targets become pathed, awk/gawk/yq/perl DSLs become opaque, sed stays read-only only for provably-print scripts, old-style tar bundles are recognized), and git read classification must account for arguments (fetch, reflog mutations, and branch configuration are git writes).
- Non-Goals: Full shell grammar parsing; auto-creating worktrees (guidance stays explicit); attributing writes across live scopes.
- Dependencies: T-077 classification framework.
- Expected Deliverables: Argument-verified allowlist, extended path extraction, corrected git rules, failing-first regressions for every audit reproduction, updated prior assertions with rationale, template parity, docs.
- Definition of Done: All reproductions blocked beside peers, verified read-only staples still pass, suites pass, independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Failing regressions for sort/awk/yq/tar and git fetch/reflog/branch reproductions.
- [x] Stage 2: Argument-verified classification, path extraction, git rules; update superseded assertions.
- [ ] Stage 3: Focused/full validation and independent gate.

## Stage Log
- 2026-07-20 00:51:46 Argument-verified allowlist (sort/uniq/base64 outputs pathed, awk/yq/perl opaque, sed print-only, old-style tar), git fetch/reflog-mutation/branch-config now writes; superseded perl assertions updated with rationale.
- 2026-07-20 00:48:00 Argument-verified allowlist (sort/uniq/base64 outputs pathed, awk/yq/perl opaque, sed print-only, old-style tar), git fetch/reflog-mutation/branch-config now writes; superseded perl assertions updated with rationale.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Read-only allowlist is now argument-verified: sort/uniq/base64 output targets are path-checked, awk/yq/inline-perl are opaque DSLs, sed is read-only only for print-only scripts, old-style tar bundles are recognized; git fetch, reflog mutations, and branch creation/deletion/configuration are git writes with listing forms still read-only.
- Tests: 2 audit-reproduction regressions red->green; focused 57/57; full discover 130/130; py_compile OK; git diff --check OK; agentctl check manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-20 01:03:31
- Completed-at-ns: 1784480611349937000
