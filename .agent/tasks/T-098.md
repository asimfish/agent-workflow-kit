# T-098 - enforce scope on loop check commands

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-20 14:10:39
Updated: 2026-07-20 14:10:39

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Close a loop-command scope-escape channel. loop run/auto/cycle execute an arbitrary shell command outside the PreToolUse guard, so a loop's Check command could write into a peer's scope (verified: echo > peer/file landed). Classify and scope-check loop commands with the same rules as tool writes, gated on a live peer (solo keeps freedom).
- Non-Goals: Restricting solo-session loops; changing built-in loops; sandboxing.
- Dependencies: T-075 classifier; T-088 scope containment; T-092 peer gating.
- Expected Deliverables: A loop-command scope guard reusing the hook classifier, failing-first peer-scope + solo-freedom regressions, docs.
- Definition of Done: A loop command writing into a peer scope fails the loop and writes nothing; solo loops unrestricted; built-in loops and cycle lifecycle unaffected; suites pass; independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Failing regression that a loop command writes into a peer scope.
- [x] Stage 2: Scope-check loop commands (peer-gated) reusing the hook classifier; add solo-freedom regression; docs.
- [x] Stage 3: Focused/full validation and independent gate.

## Stage Log
- 2026-07-20 14:29:18 Loop Check commands are now scope-checked (peer-gated) with the same classifier as tool writes, closing a cross-session write channel; solo loops keep full freedom; built-in loops and cycle lifecycle unaffected.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Loop Check commands are scope-checked (peer-gated) with the same classifier as tool writes: a loop command writing into a peer scope, via traversal, or opaque beside a peer fails the loop and writes nothing; solo loops and read-only/own-scope commands keep full freedom; built-in loops and cycle lifecycle unaffected.
- Tests: loop peer-scope + solo-freedom regressions; loop+guidance 53/53; full discover 139/139; py_compile OK; git diff --check OK; agentctl check manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-20 14:43:28
- Completed-at-ns: 1784529808682226000
