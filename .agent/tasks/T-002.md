# T-002 - add minimal loop runtime

Status: review
Owner: codex
Agent: codex
Created: 2026-07-01 16:29:30
Updated: 2026-07-01 16:29:30

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Add a minimal, project-level loop runtime that encodes the six required loop links: Trigger, Execute, Check, Feedback, Memory, and Next.
- Non-Goals: Do not add cron scheduling, worktree pools, external connectors, or auto-launching long-running tasks in this phase.
- Dependencies: Existing `.agent` workflow files, `agentctl`, installed project templates, and documentation.
- Expected Deliverables: Loop contract templates under `.agent/loops/`, installed template copies, `agentctl loop list/show/run --once`, run report generation, and docs explaining the minimal loop model.
- Definition of Done: A fresh install contains loop templates; `agentctl loop run <id> --once` creates a factual run report; built-in `daily-plan-triage`, `experiment-monitor`, and `doc-hygiene` loops can run without mutating unrelated files; checks pass.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/, templates/project/.agent/, tools/agentctl.py, README.md, docs/`
- Files likely to touch: `.agent/loops/`, `templates/project/.agent/loops/`, `tools/agentctl.py`, `README.md`, `docs/loop-engineering.md`, `docs/workflow.md`
- Files explicitly out of scope: SafeTransport/SafeOT project files, cron integrations, GitHub Actions beyond docs, and multi-worktree orchestration.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Add the loop contract documents and installed templates.
- [x] Stage 2: Implement `agentctl loop list/show/run --once` with run reports.
- [x] Stage 3: Document the interaction model and verify install/runtime behavior.

## Stage Log
- 2026-07-01 16:39:57 Final verification passed: py_compile, loop list, manual workflow check, git diff --check, and fresh install smoke test with loop run all succeeded.
- 2026-07-01 16:38:27 Implemented loop runtime and templates; install smoke test confirmed loops install, task creation writes T-002 row despite template examples, daily-plan-triage writes a run report, and manual check passes.
- 2026-07-01 16:36:44 Loop feedback closed one real issue: daily-plan-triage found T-002 missing from TASKS.md, agentctl task-create detection was fixed, current task index/plan row was added, and the next triage run passed.
- 2026-07-01 16:34:16 Added minimal loop contract files and parser/runtime skeleton: loop list/show works and validates Trigger/Execute/Check/Feedback/Memory/Next sections.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 -m py_compile tools/agentctl.py`
  - `python3 tools/agentctl.py loop list`
  - `python3 tools/agentctl.py loop run daily-plan-triage --once`
  - temporary install smoke test for `.agent/loops/`
- Commands run:
  - `python3 -m py_compile tools/agentctl.py`
  - `python3 tools/agentctl.py loop list`
  - `python3 tools/agentctl.py loop show daily-plan-triage --json`
  - `python3 tools/agentctl.py loop run daily-plan-triage --once`
  - `python3 tools/agentctl.py loop run doc-hygiene --once`
  - `python3 tools/agentctl.py loop run experiment-monitor --once`
  - temporary install smoke test with `install.sh`, `task create T-002`, `work`, `loop run daily-plan-triage --once`, and `agentctl check --mode manual`
  - `python3 tools/agentctl.py check --mode manual`
  - `git diff --check`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Added a minimal one-shot loop runtime with loop contracts, run reports, built-in triage/doc/experiment loops, installed templates, and documentation.
- Tests: python3 -m py_compile tools/agentctl.py; python3 tools/agentctl.py loop list; python3 tools/agentctl.py loop show daily-plan-triage --json; python3 tools/agentctl.py loop run daily-plan-triage --once; python3 tools/agentctl.py loop run doc-hygiene --once; python3 tools/agentctl.py loop run experiment-monitor --once; python3 tools/agentctl.py check --mode manual; git diff --check; fresh install smoke test
- Completed-at: 2026-07-01 16:40:10
