# T-061 - close T-060 identity policy review findings

Status: done
Owner: codex
Agent: codex
Created: 2026-07-19 02:11:37
Updated: 2026-07-19 02:44:35

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Close both T-060 P2 findings so every supported AgentCTL shell invocation is recognized by PreToolUse and untrusted `sessions list` performs no file write.
- Non-Goals: Changing controller command semantics, session-key hashing, trusted session rendering, migration actions, provider dispatch, or adding a hostile-shell security boundary beyond normal AgentCTL invocation forms.
- Dependencies: T-057/T-059 identity enforcement, T-060 independent reproductions, the controller's default-deny command policy, and source/template hook parity.
- Expected Deliverables: Module-form (`python -m tools.agentctl`) command classification with fail-closed handling for ambiguous AgentCTL-looking invocations; identity-aware `sessions list` rendering that remains read-only for terminal/default identity; exact regressions for both findings; concise documentation aligned with actual behavior.
- Definition of Done: Canonical and module-form mutating commands are blocked by PreToolUse and direct controller under terminal/default identity; terminal/default `sessions list` does not create or change `.agent/state/SESSIONS.md`; trusted `sessions list` still refreshes the view; focused/full scrubbed tests, static/manual checks, GitNexus audit, and a new independent gate approve T-057, T-059, and T-061.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/tools/, tests/, README.md, docs/, .agent/`
- Files likely to touch: `tools/agent_workflow_hook.py`, `templates/project/tools/agent_workflow_hook.py`, `tools/agentctl.py`, `tests/test_multi_session_workflow.py`, identity-enforcement documentation, and task/gate evidence.
- Files explicitly out of scope: Session hashing and provider identity derivation, unrelated loop/worktree/guidance behavior, destructive migration or release, Git history, and project paths outside the declared scope.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Add failing exact regressions for module-form hook classification and terminal-only `sessions list` file writes.
- [x] Stage 2: Implement minimal classifier and read-only-list fixes with source/template parity.
- [x] Stage 3: Run focused/full scrubbed verification, GitNexus audit, and a new independent reviewer gate.

## Stage Log
- 2026-07-19 02:44:35 Independent Reviewer 2 independently reproduced the required boundary conditions and found both T-060 P2 findings closed with no actionable residual issue.
- 2026-07-19 02:20:43 Closed both T-060 P2 reproductions: module-form AgentCTL commands now share the default-deny hook policy, and terminal/default sessions list is lock-free and write-free while trusted rendering remains intact. Focused 38/38 and scrubbed full 111/111 pass; static/manual/doctor/migrate/parity checks pass; GitNexus remains CRITICAL at 110 symbols and 47 flows.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-07-19 02:12:30 Accepted both independently verified T-060 P2 findings and bounded the follow-up to hook invocation recognition plus untrusted sessions-list rendering.
- 2026-07-19 02:16:00 Both exact regressions failed on the reviewed candidate, then passed after module-form classification and a lock-free/read-only untrusted sessions-list path; expanded all 49 command leaves to both invocation forms.

## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 -m unittest -v tests.test_multi_session_workflow tests.test_migrate_workflow tests.test_install_and_hooks`
  - scrubbed `python3 -m unittest discover -s tests -p 'test_*.py' -v`
  - `python3 -m py_compile` for changed Python files
  - `git diff --check`
- Expected result:
  - Both T-060 adversarial probes fail closed without authoritative or generated-state writes, trusted behavior remains compatible, and all checks pass.

### Independent Reviewer 2

- Fresh-install canonical and spaced module-form task creation each exited 2 and was hook-blocked without a task write. Terminal/default `sessions list` preserved hashes, sizes, and mtimes for project and Git-common runtime state, while trusted identity refreshed the generated view and lock without creating a session record.
- All 49 argparse leaves matched controller, script-path hook, and module-form hook policy; ambiguous module invocation failed closed and both help forms remained usable. Focused 38/38 and environment-scrubbed full 111/111 tests plus static/manual/doctor/migrate/parity checks passed.

## Completion Record
- Summary: Recognized script-path and python -m tools.agentctl invocations under one default-deny hook classifier, made terminal/default sessions list entirely read-only without lock or generated-view writes, preserved trusted view refresh, and added exact plus 49-leaf dual-form regressions and aligned documentation.
- Tests: T-060 red probes reproduced then fixed; focused identity/migration/install 38/38; full suite with 15 identity variables removed 111/111; py_compile pass; git diff --check origin/main pass; manual check OK; doctor OK; migrate=continue; source/template hook cmp pass; GitNexus 110 symbols/47 flows CRITICAL pending independent gate.
- Worker-runtimes: host-runtime:8188dc0a82f9eda0bd147ed3c18c35eb
- Completed-at: 2026-07-19 02:20:51
- Completed-at-ns: 1784398851405506000
