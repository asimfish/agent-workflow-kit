# TA08B0CC413F151F5-036 - adoption commit: the commit that installs the kit passes the kit's own hooks

Status: in_progress
Owner: cursor
Agent: cursor
Created: 2026-09-05 19:52:02
Updated: 2026-09-05 19:52:02

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal:
- Non-Goals:
- Dependencies:
- Expected Deliverables:
- Definition of Done:

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/agentctl.py, tests/test_day_one_walkthrough.py, README.md, README_CN.md, docs/install-and-upgrade.md, CHANGELOG.md`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1:
- [ ] Stage 2:
- [ ] Stage 3:

## Stage Log
- 2026-09-05 21:10:14 Second finding from the same replay, machine two: a fresh clone has .githooks/ and .gitattributes in the tree but core.hooksPath and the merge driver are clone-local config, so the first agent worked with no hooks active and nothing said so. work/start now call _wire_clone before any task state is written: set core.hooksPath=.githooks and register the driver when free, refuse (rc 2) naming 'agentctl init .' when core.hooksPath points elsewhere. Also from the replay: README reviewer step lacked the reviewer's own finish (review task stayed in_progress; close-decided-reviews only closes status=review), run stop needs --reason, claim example now passes --type code so the worktree sentence is true of it, and the dirty-planning-checkout worktree refusal names the paths or says 'agentctl sync' when only ledger data is dirty. Full README walkthrough (install, claim, overlap refusal, run start/stop with gpu lease, finish, reviewer, sync to a second clone, [elsewhere], takeover, worktree task) replayed end to end on the fixed kit.
- 2026-09-05 20:07:05 Root cause: the adoption baseline (adoption.json) pointed at the pre-install HEAD, so the install commit itself fell under the task rules; a human in a plain terminal cannot claim a task (no conversation identity) and T-000's scope is .agent/ only, so the only way through was --no-verify on commit and again on push. Fix: the commit that first adds .agent/install-manifest.json is the adoption commit; pre-commit/commit-msg/pre-push accept it without a task iff every changed path is one init writes or merges and managed files match the manifest hashes; extras/deletions refused by name; pre-push judges commit content; kit source checkout excluded (no manifest there). init prints the git add/commit lines on first install.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record

- Summary:
- Tests: not run
- Artifacts: none
- Follow-ups: none
