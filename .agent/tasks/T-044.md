# T-044 - Review T-043 signing-key binary write

Status: review
Owner: supervisor
Agent: supervisor
Created: 2026-07-18 02:33:00
Updated: 2026-07-18 02:33:00

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal:
- Non-Goals:
- Dependencies:
- Expected Deliverables:
- Definition of Done:

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1:
- [ ] Stage 2:
- [ ] Stage 3:

## Stage Log
- 2026-07-18 02:39:18 T-044 review: no findings. Focused regression OK; 20/20 guidance tests OK; py_compile OK; diff --check OK; manual check OK; in-memory O_BINARY mutant confirms test fails without the flag; PR #7 Windows failure (receipt HMAC mismatch on fresh keys) matches the fix. Short-write loop and FileExistsError->read-existing fallback preserve prior semantics.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Independent review of T-043 Windows signing-key binary write. No findings. Verified: _create_binary_secret adds O_BINARY via getattr (POSIX no-op), loops short writes over a memoryview, fsyncs, and falls back to read-existing on FileExistsError, preserving prior guidance/eval semantics. In-memory O_BINARY-stripped mutant showed the new regression's flag assertion fails without the fix. PR #7 Windows failure (receipt HMAC mismatch on freshly generated keys) matches the fixed root cause.
- Tests: python3 -m unittest tests.test_guidance_workflow.GuidanceWorkflowRegressionTest.test_signing_keys_use_binary_mode_and_preserve_exact_bytes -v (OK); python3 -m unittest tests.test_guidance_workflow -v (20 OK); python3 -m py_compile tools/agentctl.py tests/test_guidance_workflow.py (OK); git diff --check (OK); python3 tools/agentctl.py check --mode manual (OK); in-memory O_BINARY mutant check (test fails without flag)
- Worker-runtimes: host-runtime:2b75f7e88c6267eaaab20221c60aee2a, host-runtime:afd7ee3b54777834933f5dcb2f98098a
- Completed-at: 2026-07-18 02:45:08
- Completed-at-ns: 1784313908459690000
