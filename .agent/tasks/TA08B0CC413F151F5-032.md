# TA08B0CC413F151F5-032 - two-clone test fixture: pin the bare origin HEAD to main so clones are not empty on CI

Status: done
Owner: cursor
Agent: cursor
Created: 2026-09-05 03:59:42
Updated: 2026-09-05 03:59:42

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: make tests/test_multi_checkout_sync.py pass on CI, where `git init --bare` names its default branch master.
- Non-Goals: no product code change.
- Dependencies: TA08B0CC413F151F5-031 (the suite under fix).
- Expected Deliverables: the fixture sets the bare origin's HEAD to refs/heads/main after the seed push and clones with `-b main`.
- Definition of Done: the suite passes with `init.defaultBranch=master` forced locally and on CI.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tests/test_multi_checkout_sync.py`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: reproduce (CI: all eleven tests failed in setUp with an empty clone)
- [x] Stage 2: fixture fix, verified with GIT_CONFIG init.defaultBranch=master
- [ ] Stage 3: review, CI

## Stage Log
- 2026-09-05 04:00:44 fixture: bare origin HEAD -> main; clone -b main

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-09-05 04:05:00 PR #58 CI failed all eleven two-clone tests in setUp: the bare origin's HEAD pointed at master (host default) while the seed was pushed to main, so `git clone` produced an empty checkout and `init` left every file untracked. Fixed by `git symbolic-ref HEAD refs/heads/main` on the bare repo and `clone -b main`; verified locally with `GIT_CONFIG_KEY_0=init.defaultBranch GIT_CONFIG_VALUE_0=master`.

## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Two-clone fixture pins the bare origin HEAD to main and clones -b main so the suite passes where git's default branch is master (CI).
- Tests: tests/test_multi_checkout_sync.py -k install_registers with GIT_CONFIG init.defaultBranch=master: 1 passed; CI on PR #58 is the full check
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-09-05 04:01:26
- Completed-at-ns: 1788552086515503000
