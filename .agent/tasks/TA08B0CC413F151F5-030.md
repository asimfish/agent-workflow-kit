# TA08B0CC413F151F5-030 - post-review hardening: task-id references in commits, Windows CI test list, doctor report for lock records without a resource, docs count

Status: review
Owner: cursor
Agent: cursor
Created: 2026-09-04 22:47:49
Updated: 2026-09-04 22:47:49

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: close the small, non-blocking findings left by the independent reviews of #53-#56 so they do not accumulate.
- Non-Goals: no change to lease, gate, or lock semantics.
- Dependencies: none.
- Expected Deliverables: TASK_ID_RE with a negative lookbehind and Refs-trailer-scoped existence checks in _check_prepush (tests in tests/test_pre_push_hook.py); Windows CI runs the two -027 tests; doctor reports lock records without a resource name (test); docs count fix; CHANGELOG.
- Definition of Done: affected suites and the full suite pass; independent review approves; PR merged with green CI.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/agentctl.py, tests/test_pre_push_hook.py, tests/test_resource_lock_recovery.py, docs/multi-session-execution.md, CHANGELOG.md, .github/workflows/agent-workflow-check.yml`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: task-reference parsing (regex boundary, multi-segment ids, Refs trailer scope) with six tests
- [x] Stage 2: Windows CI list, doctor resource-less record finding, docs count, CHANGELOG
- [ ] Stage 3: full suite, independent review, PR, CI, merge

## Stage Log

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-09-03 06:30:00 Origin of the reference fix: pushing -028's second commit was refused because its message said "non-UTF-8" and the hook resolved "UTF-8" as a task id. `\b` accepts a hyphen as a boundary; replaced with `(?<![A-Za-z0-9-])`. Existence checks now use the ids on `Refs:` lines when a commit has that trailer (prose tokens like SHA-256 are ignored), and fall back to every id-shaped token otherwise, so a typo on the trailer is still refused and a commit without any id is still refused.
- 2026-09-03 06:50:00 Windows job now lists test_await_supervisor_claim_sees_through_a_zombie_supervisor and test_supervisor_argv_survives_tokens_that_start_with_a_dash (reviewer 027 note). doctor reports a lock record that names no resource (reviewer 028c note). docs/multi-session-execution.md interlock section says four lines (reviewer 028c note). pre-push, lock-recovery, and merge-back suites: 28 passed.
- 2026-09-03 07:05:00 While checking the new boundary, found that multi-segment ids were never matched correctly: the old `\b` boundary read `TR024-REVIEW-001` as the non-existent `REVIEW-001`, and the lookbehind alone matched nothing. TASK_ID_RE now accepts middle segments (`[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-\d+`), with a test for TR024-REVIEW-001 / T6F9B5FAE68AA3A9E-001 / T023R-001. pre-push suite: 9 passed.

## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Post-review hardening: TASK_ID_RE no longer treats tokens glued to a letter/hyphen as ids and keeps multi-segment ids whole (TR024-REVIEW-001); pre-push resolves only Refs-trailer ids when a trailer exists, otherwise every id-shaped token as before; Windows CI runs the zombie-supervisor and dash-token tests; doctor reports lock records without a resource name; interlock docs count corrected; CHANGELOG.
- Tests: pytest tests/: 243 passed (0:22:36 under load); tests/test_pre_push_hook.py: 9 passed; YAML parses
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-09-04 23:42:37
- Completed-at-ns: 1788536557292266000
