# T-081 - close newline, redirect, mv-source, and shared-ref gaps

Status: done
Owner: cursor
Agent: cursor
Created: 2026-07-20 02:52:46
Updated: 2026-07-20 02:52:46

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Close four self-audit gaps: newline-separated commands must be segmented like `;`, `&>`/`&>>`/`>|` redirects must be recognized and path-extracted, mv must treat source paths as writes (rename deletes the source), and git operations on shared refs (branch delete/rename, reflog expire/delete, fetch --prune, gc/prune, tag delete, forced/deleting push) must require exclusivity across ALL checkouts of the repository, not just the current one. Also strip timeout/nice/stdbuf/setsid wrappers before classification to avoid false opaque verdicts.
- Non-Goals: Full shell grammar; changing per-checkout semantics of ordinary commit/add.
- Dependencies: T-079 classification framework.
- Expected Deliverables: Failing-first regressions for each reproduction, segmentation/redirect/extraction fixes, a git shared-write classification enforced repo-wide, wrapper stripping, template parity, docs.
- Definition of Done: Reproductions blocked, wrapper false-positives resolved, suites pass, independent gate approves.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/, tests/, README.md, docs/, .agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: Failing regressions for newline, ampersand-redirect, mv-source, shared-ref, and wrapper cases.
- [x] Stage 2: Segmentation, extraction, shared-write enforcement, wrapper stripping.
- [ ] Stage 3: Focused/full validation and independent gate (also deflaked two load-sensitive loop/guidance timings).

## Stage Log
- 2026-07-20 04:21:11 Closed self-audit gaps: newline segmentation, ampersand/noclobber redirects, mv source writes, wrapper stripping, and repo-wide exclusivity for shared-ref git mutations.
- 2026-07-20 04:02:18 Closed self-audit gaps: newline segmentation, ampersand/noclobber redirects, mv source writes, wrapper stripping, and repo-wide exclusivity for shared-ref git mutations.
- 2026-07-20 03:40:38 Closed self-audit gaps: newline segmentation, ampersand/noclobber redirects, mv source writes, wrapper stripping, and repo-wide exclusivity for shared-ref git mutations.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Newlines segment like semicolons, ampersand/noclobber redirects are recognized and extracted, mv sources count as writes, timeout/nice/stdbuf/setsid wrappers are stripped before classification, and shared-ref git mutations (branch delete/rename, reflog expire, pruning fetch, gc, tag delete, forced/deleting push, worktree remove) require repo-wide exclusivity; deflaked two load-sensitive timings.
- Tests: 2 self-audit regressions red->green; focused 59/59; full discover 132/132 on clean rerun; deflaked tests 3x OK; py_compile OK; git diff --check OK; agentctl check manual OK
- Worker-runtimes: host-runtime:3890c45fd53b77397334c7ae183c7667
- Completed-at: 2026-07-20 06:23:12
- Completed-at-ns: 1784499792876077000
