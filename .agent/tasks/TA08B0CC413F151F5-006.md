# TA08B0CC413F151F5-006 - portable kill signal for windows run termination

Status: review
Owner: cursor
Agent: cursor
Created: 2026-08-12 05:01:39
Updated: 2026-08-12 05:01:39

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal:
- Non-Goals:
- Dependencies:
- Expected Deliverables:
- Definition of Done:

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1:
- [ ] Stage 2:
- [ ] Stage 3:

## Stage Log
- 2026-08-12 05:06:38 Windows CI runtime verification caught a real defect on its first run: signal.SIGKILL does not exist on Windows, so _signal_run_process raised AttributeError on every run stop (and the watchdog force-kill would crash the supervisor). Introduced PORTABLE_SIGKILL = getattr(signal, 'SIGKILL', 9), routed the nt taskkill branch and both kill sites through it, added a portable unit test asserting taskkill /T (+/F) routing under a mocked nt, and improved diagnosability: run wait now prints the supervisor log path when a supervised lease resolves exited_unknown, and the resource-release regression uses the diagnostics-carrying wait helper.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Fixed the Windows-only crash the new CI coverage exposed: every run stop raised AttributeError because signal.SIGKILL does not exist on Windows. A portable kill constant now routes the taskkill branch and both force-kill sites, run wait points at the supervisor log when a supervised lease resolves exited_unknown, and a mocked-nt unit test locks the taskkill routing.
- Tests: execution-lease file 17/17 in 71.4s locally; new taskkill routing unit test plus stop regression 2/2 (5.0s); py_compile OK; Windows runtime proof lands on PR #35's windows-dispatch-check
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-08-12 05:06:39
- Completed-at-ns: 1786482399548873000
