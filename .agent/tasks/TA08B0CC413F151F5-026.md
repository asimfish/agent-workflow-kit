# TA08B0CC413F151F5-026 - day-one acceptance fixes: doctor without identity, artifact gitignore, reviewer hints, README accuracy

Status: done
Owner: cursor
Agent: cursor
Created: 2026-09-02 08:19:51
Updated: 2026-09-02 08:19:51

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: close the gaps found by replaying the README day-one walkthrough verbatim on a blank project (`install.sh` into a fresh repo, three concurrent conversations, a dead GPU holder, an independent reviewer).
- Non-Goals: no change to lease semantics, gate policy, or worktree isolation; no PATH shim for `agentctl`.
- Dependencies: none (builds on merged #51/#52).
- Expected Deliverables: `doctor` in `IDENTITY_FREE_COMMAND_PATHS` (controller + both hook copies); `init` gitignores `.agent-artifacts/` with per-entry idempotence; `finish` hint and gate refusals name `agents add --id <reviewer> --role review`; README/README_CN/docs/workflow.md/CHANGELOG corrected; regression tests.
- Definition of Done: new tests pass, full suite passes (macOS-flaky watchdog test excepted, CI authoritative), independent review approves, PR merged.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/tools/, tests/, README.md, README_CN.md, CHANGELOG.md, docs/workflow.md`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: replay README walkthrough on a blank project; record every prose/tool mismatch
- [x] Stage 2: controller fixes (doctor identity-free, gitignore managed entries, reviewer hints)
- [x] Stage 3: documentation fixes (README, README_CN, docs/workflow.md, CHANGELOG)
- [x] Stage 4: regression tests in tests/test_day_one_walkthrough.py; full suite
- [ ] Stage 5: finish, independent review, merge-back, PR, CI, merge

## Stage Log
- 2026-09-02 21:19:27 fixes + tests + docs in place; full suite running in background; task doc stages 1-4 checked

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-09-02 07:25:00 Fresh-install replay: same-task, overlapping-scope refusals, shared board, dead-holder recovery (`sessions release` frees the GPU lease), supervised run, finish -> independent approve -> done, pre-commit/pre-push refusals all behaved as documented.
- 2026-09-02 07:50:00 Mismatches found: `agentctl` not on PATH and README never says it is shorthand; `doctor` refused from a plain terminal (identity required) although README step 5 says the human runs it; README `run start --output outputs/T-001/` rejected (must be task scope or `.agent-artifacts/<task>/`); README step 4 omits reviewer registration (`agents add --id <r> --role review`) and the `finish`/gate messages did not name it; `.agent-artifacts/` not gitignored by `init`; README test count stale (208 vs 215).
- 2026-09-02 08:35:00 Fixes applied in worktree; `tests/test_day_one_walkthrough.py` (7 cases) passes. Resource locks are machine-wide by design, so the suite sets `AGENT_WORKFLOW_RESOURCE_LOCK_DIR` like the existing lease tests.

## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 -m pytest tests/test_day_one_walkthrough.py -q`
  - `AGENT_WORKFLOW_RESOURCE_LOCK_DIR=/tmp/awk-026-locks python3 -m pytest tests/ -q`
  - In a fresh project with no identity env: `python3 tools/agentctl.py doctor` exits 0; `git check-ignore .agent-artifacts/x` exits 0.
- Expected result:
  - Workflow checks pass; 222 tests pass on CI (the GPU watchdog fail-safe test is timing-flaky on local macOS only).

## Completion Record
- Summary: Day-one replay fixes: doctor identity-free (controller + both hook copies); init gitignores .agent-artifacts/ with per-entry idempotent re-runs; finish hint and gate/guidance refusals name 'agents add --id <reviewer> --role review'; README/README_CN use the artifact root in run start, show reviewer registration, explain the agentctl shorthand, test count 222; docs/workflow.md reviewer prerequisites; CHANGELOG.
- Tests: python3 -m pytest tests/ -q: 222 passed (0:10:56); tests/test_day_one_walkthrough.py: 7 passed; py_compile OK
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-09-02 21:29:54
- Completed-at-ns: 1788355794195893000
