# TA08B0CC413F151F5-037 - task contract as the audit surface: Definition of Done required at finish, tests as a re-runnable command

Status: review
Owner: cursor
Agent: cursor
Created: 2026-09-06 02:43:47
Updated: 2026-09-06 02:43:47

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: make the task contract the thing a reviewer judges against, and make test evidence a command the tool runs instead of a sentence the worker writes
- Non-Goals:
- Dependencies:
- Expected Deliverables:
- Definition of Done: finish refuses when Definition of Done is empty (review tasks satisfied by a recorded gate decision); --goal/--done/--tests-cmd accepted by task create, work --auto-create, and a new 'contract' command; finish executes --tests-cmd (or the contract's Tests command), refuses on non-zero exit, records Tests-command/Tests-exit/Tests-duration; gate approve --rerun-tests re-executes it and refuses on failure, and the gate record carries DoD, command, and rerun result; template, WORKFLOW_ENTRY, README x2, docs/workflow.md, CHANGELOG updated; regression tests for each rule

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/agentctl.py, templates/project/.agent/tasks/_template.md, templates/project/.agent/WORKFLOW_ENTRY.md, .agent/WORKFLOW_ENTRY.md, tests/, README.md, README_CN.md, docs/workflow.md, CHANGELOG.md`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1:
- [ ] Stage 2:
- [ ] Stage 3:

## Stage Log
- 2026-09-08 20:31:03 review 037h finding fixed: every session row reaches disk through _write_session_row, which records the task-keyed runtimes; sessions heartbeat and sessions release no longer bypass it; 2 tests (heartbeat-only joiner refused in the no-finish and handoff shapes) -> 39 in test_task_contract.py, 311 total; README/README_CN/CHANGELOG list heartbeat too; eight-suite regression 141 passed
- 2026-09-07 15:45:33 review 037f finding fixed: every save of a task-bound session (claim, note, refresh, release, finish) appends the session runtimes to the task-keyed record, not only finish; two tests (no-finish hand-written record refused; starter of a handed-off task cannot review it) -> 37 in test_task_contract.py, 309 total; README/README_CN/CHANGELOG say exactly that; ten session-heavy suites 170 passed; reviewer 037f's no-finish scenario reproduced by hand: refused
- 2026-09-07 05:59:01 review 037e finding fixed: finish persists the worker runtimes in a task-keyed record under the git common dir (.git/agent-workflow/task-runtimes/<task>.json) that no later work of the same session overwrites; the gate unions it; README/README_CN/CHANGELOG sentence corrected to what holds; cmd_start validates the agent name; plan bullets rendered from flattened board fields; finish re-checks the record structure under the lock after the tests command ran; 2 new tests (35 in test_task_contract.py, 307 total); eight-suite regression 112 passed; same-session value-edit self-review reproduced by hand: refused
- 2026-09-07 01:14:57 hardened test_timeout_kills_the_whole_command_not_just_the_shell: poll up to 5s for the killed group to leave the process table (finish's own tests-command run hit the race once under load)
- 2026-09-07 00:57:15 review 037c findings fixed: one case-insensitive header predicate shared by reader, tamper check and finish; note/title/owner/takeover reason flattened to one line; agents add validates ids; Stage Log located as a line; finish refuses a misplaced or duplicated record instead of rewriting; README/README_CN/docs/enforcement.md state the cross-machine boundary of the runtime check; 4 new tests (test_task_contract.py now 33); nine-suite regression 146 passed
- 2026-09-06 23:11:56 review 037b findings fixed: one RECORD_CONTROL_RE predicate (all splitlines boundaries + C0/DEL) refused for tests commands and collapsed by every field writer; task docs read as physical lines only; Completion Record must be the last and only such section (gate + evidence check refuse otherwise); finish finds the record header as a line; gate unions this checkout's session runtimes for the task; gate note single-line; bad tests command refused before worktree bootstrap; 6 new tests (31 in test_task_contract.py); impacted suites 95 passed
- 2026-09-06 17:50:35 review 037 findings fixed: single-line tests commands at every entry, gate reads every Worker-runtimes line, contract checks the receipt first, gate rerun before the lock with re-check under it; verbatim command storage, process-group kill on timeout, indented DoD read; 7 regression tests; test_task_contract 23 passed, touched suites 66 passed
- 2026-09-06 04:20:22 contract fields at create/contract/finish; finish executes --tests-cmd and refuses on failure; gate --rerun-tests; template/entry/README/docs/CHANGELOG; 16 new tests, 33 existing finish call sites given --done; part-1 regression 95 passed

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.
- Tests command: `python3 -m pytest tests/test_task_contract.py tests/test_install_and_hooks.py tests/test_day_one_walkthrough.py -q -p no:cacheprovider`

## Completion Record

- Summary: Task contract is the audit surface (seventh version): Definition of Done required at finish (review tasks satisfied by a recorded gate decision); --goal/--done/--tests-cmd on task create, work --auto-create (validated before worktree bootstrap), the 'contract' command (receipt checked before writing), and finish; one RECORD_CONTROL_RE predicate covers every control and line-boundary character (writers collapse, tests commands refuse); every prose field the CLI writes into a task document is one physical line and agent ids are validated names; task documents are read as physical lines; one case-insensitive header predicate shared by the section reader, the tamper check and finish's record locator; the completion record must be the last and only such section or gate, evidence check and finish (checked again under the lock) refuse; finish executes the tests command (process group killed on timeout) and records Tests-command/Tests-exit/Tests-duration; every session row reaches disk through one writer that appends the row's runtimes to a task-keyed record under the git common dir (claim, heartbeat, note, refresh, release, finish), which no later work of the same key overwrites; gate approve --rerun-tests reruns before the lock, re-checks the command under it, reads every Worker-runtimes line and unions the task-keyed record and live session records, so inside one repository every runtime that ever held a session on the task is refused as its reviewer; READMEs state exactly that and the cross-clone boundary (docs/enforcement.md wording is a follow-up docs task, outside this scope); template, WORKFLOW_ENTRY, READMEs, docs/workflow.md, CHANGELOG updated
- Tests: tests/test_task_contract.py collects 39 and passes (16 original, 7 for review 037, 6 for 037b, 4 for 037c, 2 for 037e, 2 for 037f, 2 for 037h: a runtime that only heartbeat under the session is remembered, in the no-finish and the handoff shape); eight-suite regression (contract, multi_session_workflow, multi_session_e2e, install_and_hooks, read_receipt, day_one, multi_checkout_sync, upgrade_barrier) 141 passed; collect-only 311 matches both READMEs
- Tests-command: python3 -m pytest tests/test_task_contract.py tests/test_install_and_hooks.py tests/test_day_one_walkthrough.py -q -p no:cacheprovider
- Tests-exit: 0
- Tests-duration: 711.4s
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-09-08 20:43:43
- Completed-at-ns: 1788871423818931000
