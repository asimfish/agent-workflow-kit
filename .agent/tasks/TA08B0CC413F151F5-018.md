# TA08B0CC413F151F5-018 - resource and worktree lease deadlock self-healing and diagnostics

Status: done
Owner: cursor
Agent: cursor
Created: 2026-08-25 06:11:12
Updated: 2026-08-25 06:11:12

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: eliminate resource/worktree lease interlocks where a dead holder (finished run, released or vanished session) keeps a resource nobody can claim, and make every remaining conflict diagnosable and recoverable.
- Non-Goals: auto-releasing resources held by stale (heartbeat-lost) sessions; changing the fail-closed holder binding for live holders; watchdog policy changes.
- Dependencies: none (builds on the orphaned-run-resource sweep from TA08B0CC413F151F5-009).
- Expected Deliverables: generalized orphan sweep covering conversation holders; acquire-conflict self-heal and retry; holder-aware rejection diagnostics; `sessions release` resource sweep; `resource release --force-stale`; doctor interlock findings; regression tests; docs.
- Definition of Done: full test suite green; docs describe the three recovery lines (automatic sweep, actionable rejection, operator escape hatch).

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, tests/, docs/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: audit acquisition/release/session-lifecycle paths for interlock scenarios
- [x] Stage 2: implement self-healing (orphan sweep generalization, acquire retry, sessions release sweep, force-stale, doctor findings)
- [ ] Stage 3: full-suite verification, independent review, PR, CI, merge

## Stage Log
- 2026-08-26 19:29:40 Implemented interlock self-healing: generalized orphan sweep to released/missing conversation holders, acquire conflicts self-heal and retry once with actionable holder diagnostics, sessions release frees held resources, resource release --force-stale for provably dead holders (live holders refused, allow_abbrev=False keeps --force an error), doctor reports stuck resource/worktree leases. 5 new regressions; lease suite 27/27 green.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Interlock self-healing and diagnostics: generalized the orphan resource sweep to released/missing conversation holders, made contested acquires self-heal and retry once, enriched acquire rejections with holder identity/liveness/recovery command, sessions release now frees held resources, added resource release --force-stale (live holders refused; allow_abbrev=False keeps bare --force an error), and doctor now reports leases stuck without a live holder including done-task worktree leases and release_failed locks.
- Tests: python3 -m pytest tests/ -q -> 204 passed (0:10:57); new regressions cover released-session orphan sweep, acquire self-heal and diagnostics, sessions-release resource sweep, force-stale guardrails, doctor findings
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-08-26 20:28:25
- Completed-at-ns: 1787747305437688000
