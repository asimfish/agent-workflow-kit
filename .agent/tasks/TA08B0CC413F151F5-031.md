# TA08B0CC413F151F5-031 - multi-checkout ledger sync: git merge driver for ledger files, pushable ledger-only claims, explicit takeover of foreign in_progress tasks, agentctl sync

Status: blocked
Owner: cursor
Agent: cursor
Created: 2026-09-05 00:28:13
Updated: 2026-09-05 00:28:13

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: make the kit's guarantees hold across several machines that share one Git remote: claims become visible before review, concurrent ledger changes merge instead of conflicting, and a claim made on another machine cannot be taken silently.
- Non-Goals: cross-machine session liveness, cross-host GPU scheduling, and worktree leases stay per machine; no change to the review gate.
- Dependencies: builds on #55 (owner records) and #57 (Refs-trailer references).
- Expected Deliverables: `merge-driver` subcommand + `.gitattributes` managed block + per-clone driver registration in `init`; pre-push allows ledger-only commits at any status; `--takeover --reason` guard on `start`/`work --task` with board/doc/log records; `agentctl sync`; `doctor` check for the driver; docs (multi-session guide section, workflow, READMEs, CHANGELOG); two-clone regression tests.
- Definition of Done: the two-clone experiment that opened the task (two claims, two finishes, a takeover attempt) runs without conflicts and without a silent takeover; full suite passes; independent review approves; PR merged with green CI.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/, templates/project/, tests/test_multi_checkout_sync.py, tests/test_multi_session_workflow.py, docs/multi-session-execution.md, docs/workflow.md, README.md, README_CN.md, CHANGELOG.md, install.sh`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: reproduce with two clones of one bare remote (claim not pushable, silent takeover, four-file conflict on rebase)
- [x] Stage 2: merge driver + attributes + init registration; ledger-only push rule; takeover guard; sync; doctor check
- [x] Stage 3: two-clone regression tests; docs
- [ ] Stage 4: full suite, independent review, PR, CI, merge

## Stage Log

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-09-05 00:20:00 Two-clone experiment (machineA/machineB on one bare origin): A's "open task" ledger commit refused by pre-push ("must be review/approved/done"); B's `start --task` on A's in_progress task flipped the owner from codex to claude with no warning; after both finished, B's `pull --rebase` conflicted in board.json, TASKS.md, PROJECT_PLAN.md, loops/state.json.
- 2026-09-05 01:40:00 Implemented: `_three_way_entries` per-id merge with `_resolve_board_entry` (status rank then updated_at); TASKS.md rows and PROJECT_PLAN.md checklist merged the same way, plan prose via `git merge-file` with conflicts surfaced; `loops/state.json` keeps ours; `progress.md` uses git's union merge. `init` writes the .gitattributes block and `merge.agent-ledger.driver`. `_check_prepush` requires review/approved/done only for commits that change files outside .agent/. `_foreign_claim_holder`/`_foreign_claim_error`/`_record_takeover` in `cmd_start`; `work --task` forwards --takeover/--reason. `cmd_sync`. doctor check "ledger merge driver". Identity-free + upgrade allow-lists include merge-driver.
- 2026-09-05 02:10:00 tests/test_multi_checkout_sync.py: 7 passed (install registers driver; claim pushable while in_progress and code refused; foreign claim needs --takeover --reason and is recorded; concurrent finishes rebase without conflict and reconcile check passes; board resolution rules; plan prose conflict still reported; sync round trip incl. staged-code refusal).

## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Multi-checkout ledger sync: agent-ledger git merge driver (per-task three-way merge of board.json/TASKS.md/PROJECT_PLAN.md/agents.json, loops/state.json kept local, progress.md union) registered by init with a managed .gitattributes block; pre-push lets ledger-only commits travel at any task status while commits touching files outside .agent/ still wait for review; start/work --task refuse a foreign in_progress claim unless --takeover --reason, recorded in doc/log/board; agentctl sync (ledger-only commit, pull --rebase, re-render, push); doctor checks the driver; docs, READMEs, CHANGELOG; 7 two-clone tests.
- Tests: tests/test_multi_checkout_sync.py + multi_session + pre_push + merge_back + install suites: 86 passed (0:44 under load ~120); full suite running; two-clone experiment reproduced then closed
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-09-05 01:37:06
- Completed-at-ns: 1788543426954383000
