# T17F063E6115138DE-006 - integrate concurrent main advances without history loss

Status: in_progress
Owner: codex
Agent: codex
Created: 2026-07-20 14:55:14
Updated: 2026-07-20 14:55:14

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: Integrate `origin/main@ef035f5` into the approved concurrent-session
  branch without losing either line's implementation, tests, or durable task
  evidence.
- Non-Goals: Do not choose one side wholesale, discard another session's work,
  rewrite published history, or weaken the approved session/Git/loop guards.
- Dependencies: Main advanced through PRs #19-#24 after release commit
  `93133f4`; the old unnamespaced allocator allowed historical task IDs such as
  `T-088`, `T-090`, and `T-098` to be reused independently.
- Expected Deliverables: A collision ledger and stable migration for duplicate
  task/gate records, a semantic union of `agentctl` and workflow-hook changes,
  source/template parity, combined regression coverage, and a clean merge
  commit suitable for updated PRs.
- Definition of Done: Both branches' behaviors and evidence remain traceable;
  no conflict markers or duplicate active identities remain; focused and full
  suites, manual/doctor, syntax, parity, JSON, diff, and GitNexus checks pass;
  an independent reviewer approves the integration before release.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/, tools/, templates/project/, tests/, docs/, README.md, .githooks/, .claude/, .codex/, .cursor/, hooks/`
- Files likely to touch: Conflicted `.agent` indexes/records,
  `tools/agentctl.py`, `tools/agent_workflow_hook.py`, its project template,
  `docs/enforcement.md`, and combined tests.
- Files explicitly out of scope: Remote history rewriting, unreviewed feature
  expansion, and unrelated repositories or worktrees.

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1: Inventory semantic/code conflicts and historical task-ID collisions.
- [ ] Stage 2: Merge main and resolve every conflict with explicit preservation evidence.
- [ ] Stage 3: Run focused/full verification and independent integration review.

## Stage Log
- 2026-07-20 14:55:55 Main advanced through PRs #19-#24 during release; started collision-aware semantic integration instead of overwriting peer work.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
  - `python3 -m unittest discover -s tests -p 'test_*.py'`
  - `python3 -m py_compile tools/agentctl.py tools/agent_workflow_hook.py templates/project/tools/agent_workflow_hook.py`
  - `cmp tools/agent_workflow_hook.py templates/project/tools/agent_workflow_hook.py`
  - `git diff --check`
  - `npx gitnexus detect-changes --scope all`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record

- Summary:
- Tests: not run
- Artifacts: none
- Follow-ups: none
