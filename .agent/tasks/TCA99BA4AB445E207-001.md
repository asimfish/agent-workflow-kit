# TCA99BA4AB445E207-001 - independent review of portable windows kill signal

Status: done
Owner: independent-reviewer-2
Agent: independent-reviewer-2
Created: 2026-08-12 05:09:35
Updated: 2026-08-12 05:09:35

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal:
- Non-Goals:
- Dependencies:
- Expected Deliverables:
- Definition of Done:

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `.agent/`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [ ] Stage 1:
- [ ] Stage 2:
- [ ] Stage 3:

## Stage Log
- 2026-08-12 05:09:54 Independent review of de4c5e9 (task TA08B0CC413F151F5-006): APPROVE, no blockers. The fix is correct and complete. PORTABLE_SIGKILL = getattr(signal, 'SIGKILL', 9) resolves to the real SIGKILL enum (value 9) on POSIX and to int 9 on Windows, so the nt taskkill branch of _signal_run_process now evaluates {signal.SIGTERM, PORTABLE_SIGKILL} without the AttributeError that crashed every run stop on Windows (PR #35 windows-dispatch-check run 31534912239); value 9 correctly routes /F, and the final os.kill(pid, 9) fallback is also Windows-safe (TerminateProcess semantics). Completeness verified: rg 'signal.SIGKILL' tools/ shows only line 8595 inside _terminate_loop_process under an os.name == 'posix' guard, intentionally unchanged; both force-kill call sites (_settle_stopped_run_tree line 6210, watchdog kill in _run_supervise line 6445) now use the constant, and on POSIX behavior is byte-identical since PORTABLE_SIGKILL is signal.SIGKILL there. The new mocked-nt unit test locks the routing safely: os.name and subprocess.run patches are scoped to a with block, process_group=None keeps the killpg branch unreachable, fake_run returns rc 0 so no real process is touched, and it asserts both taskkill argvs plus the constant definition on the running platform. The run wait diagnosability hint fires only for status == 'exited_unknown' and lease mode == 'supervised', goes to stderr, preserves exit-code semantics, and the hinted path {lease}.supervisor.log matches the writer at line 6683; the resource-release regression now uses wait_run_with_diagnostics (helper exists, test line 595). Independently verified: python3 -m py_compile tools/agentctl.py OK; the 3 targeted tests (windows taskkill routing, owner-only stop, resource hold/release) pass 3/3 OK in 9.36s. Real-Windows proof will land on PR #35 windows-dispatch-check.

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.


## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Independent review of de4c5e9 (portable Windows kill signal, task TA08B0CC413F151F5-006): approved with no blockers. PORTABLE_SIGKILL = getattr(signal, 'SIGKILL', 9) is correct on both platforms (real enum on POSIX, 9 -> taskkill /F on nt), both force-kill call sites route through it, and the only remaining signal.SIGKILL is the posix-guarded killpg site in _terminate_loop_process. The mocked-nt unit test safely locks the taskkill routing (scoped patches, process_group=None, mocked subprocess.run). The run wait diagnosability hint fires only for supervised leases resolving exited_unknown, prints to stderr, and points at the correct {lease}.supervisor.log path. Gate approved; real-Windows confirmation expected on PR #35 windows-dispatch-check.
- Tests: python3 -m py_compile tools/agentctl.py OK; python3 -m unittest (test_signal_run_process_routes_windows_signals_through_taskkill, test_only_the_owner_conversation_can_stop_a_run, test_supervised_run_holds_resource_then_releases_it_after_success) -> Ran 3 tests in 9.360s OK; rg 'signal.SIGKILL' tools/ -> only posix-guarded line 8595
- Worker-runtimes: host-runtime:0bf62056eb1dceb04fbd1847b5afb311
- Completed-at: 2026-08-12 05:10:21
- Completed-at-ns: 1786482621783204000
