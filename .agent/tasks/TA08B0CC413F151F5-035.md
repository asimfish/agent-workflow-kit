# TA08B0CC413F151F5-035 - multi-machine day to day: sync in the agent loop with hints, run stop during the launch window and supervisor-side stop escalation, sync autostash, foreign-claim age on board and doctor

Status: blocked
Owner: cursor
Agent: cursor
Created: 2026-09-05 16:28:08
Updated: 2026-09-05 16:28:08

## Format Rules

- Keep this task doc factual and current; do not paste long reasoning transcripts.
- Preserve all top-level headings. Add subsections only under the existing headings.
- Update `Status:` through the workflow commands when possible.
- Use stable paths relative to the repo root.
- If a human edits this file, agents must re-read it and run `python3 tools/agentctl.py refresh` before continuing.

## Task Contract

- Goal: make the multi-machine mechanics from #58 part of an agent's day (guidance and reminders), give humans a cross-checkout staleness signal, and make `run stop` reliable from the moment `run start` returns.
- Non-Goals: no change to the merge driver's evidence rules or the review gate; no cross-machine liveness (impossible without a channel beyond Git).
- Dependencies: #58 (sync, takeover), #59.
- Expected Deliverables: WORKFLOW_ENTRY (template + kit copy) section "Other Checkouts And Machines" and finish step 4; `_print_sync_hint` in start/finish; `board` `[elsewhere, updated <age> ago]`; doctor check "claims from other checkouts" (FOREIGN_CLAIM_QUIET_HOURS=24); `sync` autostash + accurate docstring; `_merge_ledger_json` rejects a non-object collection; `run stop` launch-window handling (`_run_stop_before_payload`, `--wait-seconds`), supervisor pre-launch cancel, `stopping` preserved on registration, supervisor-side SIGTERM once / kill after `--kill-seconds`; docs; CHANGELOG; tests.
- Definition of Done: lease + two-clone suites pass, full suite passes, independent review approves, CI green.

## Context To Read Before Starting

- `AGENTS.md`
- `.agent/PROJECT_PLAN.md`
- `.agent/TASKS.md`
- `.agent/rules/agent-operating-rules.md`
- `.agent/rules/github-standards.md`

## Work Scope

- Allowed write scope: `tools/agentctl.py, tests/test_execution_lease_workflow.py, tests/test_multi_checkout_sync.py, templates/project/.agent/WORKFLOW_ENTRY.md, .agent/WORKFLOW_ENTRY.md, docs/workflow.md, docs/multi-session-execution.md, CHANGELOG.md`
- Files likely to touch:
- Files explicitly out of scope:

## Stage Plan

Use one checkbox per stage. Do not delete completed stages; append changed or new stages with a short reason.

- [x] Stage 1: run stop launch window + supervisor stop handling + escalation
- [x] Stage 2: sync autostash, merge validation, hints, board marker, doctor check, WORKFLOW_ENTRY guidance
- [x] Stage 3: tests and docs
- [ ] Stage 4: full suite, review, PR, CI

## Stage Log
- 2026-09-05 16:46:57 stop window + escalation, sync autostash, hints, board/doctor foreign-claim age, WORKFLOW_ENTRY guidance; tests green so far

Format: `- YYYY-MM-DD HH:MM:SS <short factual update>`.

- 2026-09-05 16:10:00 Root cause of the Windows CI flake seen on #58 (`run stop` -> "run process is not alive"): `run start` returns after the supervisor claims the lease, the payload is registered a moment later, and `_run_stop` refused when `processes` was empty. Also found: for runs without a watchdog nobody ever escalated a SIGTERM the payload ignored, and the supervisor's registration write overwrote a `stopping` status with `running`.
- 2026-09-05 16:40:00 Implemented `_run_stop_before_payload` (record stop, wait `--wait-seconds` for the payload, signal it; report if the supervisor cancels first or dies), supervisor pre-launch cancel, `register_child` preserving `stopping`, supervisor-side stop: SIGTERM once unless `run stop` already sent it (`stop_signal_sent_at_ns`), kill after `termination_deadline_ns` (`--kill-seconds`). Tests: stop right after start (cancelled), deterministic launch-window test with a fake supervisor, SIGTERM-ignoring payload killed after 1s; owner test still passes.
- 2026-09-05 17:00:00 Multi-machine day to day: WORKFLOW_ENTRY (template and kit copy, identical) gains "Other Checkouts And Machines" and finish step 4; `_print_sync_hint` after start/finish when a remote exists; `board` marks claims with no local session `[elsewhere, updated <age> ago]`; doctor "claims from other checkouts" warns after 24h of board silence with the takeover command; `sync` pulls with `--autostash` and its docstring matches the allowlist; `_merge_ledger_json` raises when a side's `tasks` is not an object. Tests: sync autostash (dirty tracked .gitignore survives, not committed), board marker + doctor warn (never takes over), non-object collection.

## Verification

- Commands to run:
  - `python3 tools/agentctl.py check --mode manual`
- Expected result:
  - Workflow checks pass and task-specific acceptance criteria are met.

## Completion Record
- Summary: Multi-machine day to day: WORKFLOW_ENTRY tells agents to sync after claim/create/finish and how to take over a foreign claim; work/finish print the sync reminder when a remote exists; board marks foreign in_progress claims with age; doctor warns after 24h of board silence; sync autostashes unrelated edits and rejects a non-object tasks collection. run stop works from the moment run start returns (launch-window wait, supervisor pre-launch cancel, stopping preserved on registration) and the supervisor escalates a requested stop to a kill after --kill-seconds; tests and docs.
- Tests: pytest tests/: 260 passed (0:13:35); new: stop right after start, deterministic launch-window stop, SIGTERM-ignoring payload killed after 1s, sync autostash, board/doctor foreign claim, non-object collection
- Worker-runtimes: host-runtime:9b2d3c21837d49890a509a12e90e4a79
- Completed-at: 2026-09-05 17:02:00
- Completed-at-ns: 1788598920409384000
